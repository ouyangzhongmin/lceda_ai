# 用 PLANS.md 搞定复杂任务：Codex 多小时自主编程指南

Codex 搭配 `gpt-5.4` 模型（推荐），可以用来完成那些需要大量调研、设计和实现的复杂任务。本文介绍的方法，是通过一份结构化的计划文档来引导模型完成这类任务，并在漫长的实现过程中保持方向正确。

这类计划文档本质上是详尽的设计文档，同时也是"活文档"——它会随着工作推进不断更新。作为 Codex 的使用者，你可以在模型开始长时间实现之前，先通过这份文档审查它的整体方案。下面展示的 `PLANS.md` 模板，和实际让 Codex 连续工作超过七个小时的版本非常接近。

要让 Codex 用上这套机制，需要做两件事：先在 `AGENTS.md` 中告诉它什么时候该用计划文档，然后把 `PLANS.md` 文件加到仓库里。

## AGENTS.md 配置

[`AGENTS.md`](https://github.com/openai/agents.md) 是一种简洁的格式，用来给 Codex 这类编程智能体提供指引。我们在里面定义一个缩写术语，以及一条简单的规则来说明什么时候该用计划文档。这里我们把它叫做"ExecPlan"——注意这只是个自定义术语，Codex 并没有专门针对它做过训练。之后在给 Codex 下指令时，就可以用这个缩写来指向具体的计划定义。

下面是一段 `AGENTS.md` 的配置示例，告诉智能体何时使用计划文档：

```md
# ExecPlans

在编写复杂功能或进行大规模重构时，从设计到实现全程使用 ExecPlan（详见 .agent/PLANS.md）。
```

## PLANS.md 完整文档

以下是完整的 PLANS.md 文档内容。其中的提示词经过精心设计，既能给用户提供充分的反馈，又能引导模型严格按照计划执行。你可以根据自己的需求进行定制，增删相关章节。

~~~md
# Codex 执行计划（ExecPlan）

本文档定义了"执行计划"（ExecPlan）的规范——一份编程智能体可以照着做的设计文档，目标是交付一个可运行的功能或系统变更。写计划时，请把读者当作对这个仓库完全陌生的新手：他们手里只有当前的代码和你提供的这一份 ExecPlan 文件，没有之前的记忆，也没有额外的上下文。

## 如何使用 ExecPlan 和 PLANS.md

编写 ExecPlan 时，必须严格遵循 PLANS.md 的每一条要求。如果 PLANS.md 不在你的上下文中，先完整读一遍再动手。在编写规范时要充分阅读（反复阅读）源代码，确保产出的规范准确无误。建议先从骨架模板开始，边调研边填充内容。

执行 ExecPlan 时，不要停下来问用户"下一步做什么"，直接推进到下一个里程碑。每次停顿时都要更新所有章节，在列表中增加或拆分条目，明确记录已完成的进展和接下来的步骤。遇到模糊的地方自己做决策，并且频繁提交代码。

讨论 ExecPlan 时，把决策记录在规范的日志中，方便日后追溯。任何对规范的修改都应该有清晰明确的理由。ExecPlan 是活文档，任何时候都应该能仅凭这份 ExecPlan 重新开始工作，不依赖其他材料。

当面对需求复杂或存在重大未知的设计时，可以用里程碑来实现概念验证、"玩具实现"等，用来验证用户方案的可行性。要深入研究，去找到或获取相关库的源代码来阅读，并在计划中包含原型来指导后续的完整实现。

## 核心要求

以下是不可妥协的硬性要求：

* 每份 ExecPlan 必须完全自包含。自包含意味着：文档当前的内容已经包含了一个新手成功完成任务所需的全部知识和指令。
* 每份 ExecPlan 都是活文档。贡献者必须随着工作推进、新发现出现、设计决策确定而持续修订。每次修订后仍然必须保持完全自包含。
* 每份 ExecPlan 必须让一个完全不了解这个仓库的新手，能够端到端地实现整个功能。
* 每份 ExecPlan 必须产出可以实际演示的工作行为，而不仅仅是"满足定义"的代码改动。
* 每份 ExecPlan 必须用通俗语言定义所有专业术语，否则就不要用。

目的和意图永远放在第一位。开头先用几句话解释这项工作为什么重要——从用户视角出发：这个改动完成后用户能做什么之前做不到的事，以及如何看到它在工作。然后引导读者一步步达成目标，包括要编辑什么、要运行什么、应该观察到什么。

执行你计划的智能体可以列出文件、读取文件、搜索、运行项目和运行测试。但它不知道任何之前的上下文，也无法从早期的里程碑中推断你的意图。你依赖的每个假设都要重复说明。不要指向外部博客或文档；如果需要某些知识，就用自己的话嵌入到计划中。如果 ExecPlan 基于之前的某份 ExecPlan 且该文件已提交到仓库，可以通过引用方式纳入。如果没有提交，则必须把相关上下文完整包含进来。

## 格式要求

格式简单且严格。每份 ExecPlan 必须是一个标记为 `md` 的单独围栏代码块，以三个反引号开始和结束。内部不要嵌套额外的三反引号代码块；需要展示命令、输出、diff 或代码时，使用缩进块。用缩进来保持清晰，而不是在 ExecPlan 内部使用代码围栏（避免提前关闭外层围栏）。每个标题后空两行，使用 #、## 等标题语法，以及正确的有序和无序列表格式。

当把 ExecPlan 写入一个 Markdown（.md）文件，且文件内容仅包含这一份 ExecPlan 时，可以省略外层的三反引号。

用平实的散文体写作。优先用完整句子而非列表。避免使用清单、表格和长篇罗列，除非简短表达会导致含义模糊。清单格式仅允许在"进度"（Progress）章节使用，且在该章节中是必须的。叙述性章节必须以散文为主。

## 编写指南

自包含和通俗语言是最重要的。如果你引入了一个非日常用语的短语（如"守护进程"、"中间件"、"RPC 网关"、"过滤器图"），必须立即定义它，并提醒读者它在这个仓库中的具体表现形式（比如出现在哪些文件或命令中）。不要说"如前所述"或"根据架构文档"。把需要的解释写在这里，即使重复也没关系。

避免常见的失败模式。不要依赖未定义的术语。不要把功能描述得过于狭隘，导致代码能编译但实际上什么都没做。不要把关键决策推给读者。当存在歧义时，在计划中直接解决，并解释为什么选择这条路。宁可过度解释用户可见的效果，也不要过度规定无关紧要的实现细节。

用可观察的结果来锚定计划。说明实现后用户能做什么、要运行什么命令、应该看到什么输出。验收标准要描述人类可以验证的行为（比如"启动服务器后，访问 http://localhost:8080/health 返回 HTTP 200，响应体为 OK"），而不是内部属性（比如"添加了一个 HealthCheck 结构体"）。如果改动是内部的，也要解释如何演示其影响（比如运行测试，改动前失败、改动后通过，并展示一个使用新行为的场景）。

明确指定仓库上下文。用完整的仓库相对路径命名文件，精确命名函数和模块，描述新文件应该创建在哪里。如果涉及多个区域，写一段简短的导航说明，解释这些部分如何配合，让新手能自信地找到方向。运行命令时，说明工作目录和完整的命令行。当结果依赖环境时，说明假设条件，并在合理情况下提供替代方案。

保持幂等和安全。步骤要写成可以多次运行而不会造成损坏或漂移的形式。如果某个步骤可能中途失败，说明如何重试或调整。如果需要迁移或破坏性操作，详细说明备份或安全回退方案。优先采用增量的、可测试的改动，边做边验证。

验证不是可选的。包含运行测试的指令，如果适用的话还要包含启动系统的指令，以及观察它做有用事情的指令。为任何新功能或能力描述全面的测试。包含预期输出和错误信息，让新手能区分成功和失败。尽可能展示如何证明改动有效，而不仅仅是能编译（比如通过一个小型端到端场景、一个 CLI 调用、或一个 HTTP 请求/响应记录）。说明项目工具链对应的具体测试命令，以及如何解读结果。

记录证据。当你的步骤产生终端输出、简短 diff 或日志时，把它们作为缩进示例包含在单个围栏块内。保持简洁，聚焦于能证明成功的内容。如果需要包含补丁，优先使用文件级别的 diff 或小段摘录，让读者可以按照你的指令重现，而不是粘贴大段代码。

## 里程碑

里程碑是叙事，不是官僚流程。如果你把工作拆分成多个里程碑，每个里程碑都要用一段简短的文字来介绍：范围是什么、完成后会多出什么之前没有的东西、要运行什么命令、预期观察到什么验收结果。把它写成一个故事：目标、工作、结果、证据。进度和里程碑是两回事：里程碑讲故事，进度追踪细粒度的工作。两者都必须存在。永远不要为了简短而省略里程碑的细节，不要遗漏那些对未来实现可能至关重要的信息。

每个里程碑必须可以独立验证，并且是对整体目标的增量实现。

## 活文档与设计决策

* ExecPlan 是活文档。当你做出关键设计决策时，更新计划，记录决策本身和背后的思考过程。所有决策都记录在"决策日志"（Decision Log）章节。
* ExecPlan 必须包含并维护以下章节："进度"（Progress）、"意外发现"（Surprises & Discoveries）、"决策日志"（Decision Log）、"成果与复盘"（Outcomes & Retrospective）。这些不是可选的。
* 当你发现优化器行为、性能权衡、意外 bug、或影响了你方案的反向/撤销语义时，把这些观察记录在"意外发现"章节，附上简短的证据片段（测试输出最理想）。
* 如果你在实现过程中改变了方向，在"决策日志"中记录原因，并在"进度"中体现影响。计划既是你的清单，也是下一个贡献者的指南。
* 在完成一个重要任务或整个计划时，写一条"成果与复盘"记录，总结完成了什么、还剩什么、以及学到的经验。

## 原型里程碑与并行实现

在计划中明确包含原型里程碑是完全可以的——而且往往值得鼓励——当它们能降低大型改动的风险时。比如：给一个依赖库添加底层操作符来验证可行性，或者尝试两种组合顺序并测量优化器效果。保持原型是增量的、可测试的。明确标注范围为"原型"；描述如何运行和观察结果；说明将原型提升为正式方案或丢弃的判断标准。

优先采用先增后减的代码改动方式，确保测试始终通过。并行实现（比如在迁移过程中同时保留适配器和旧路径）是可以的，只要它们能降低风险或让测试在大型迁移期间继续通过。描述如何验证两条路径，以及如何在测试保护下安全地退役其中一条。当涉及多个新库或功能领域时，考虑创建独立的探针（spike），分别评估这些功能的可行性——独立于彼此来验证外部库是否按预期工作、是否实现了我们需要的功能。

## ExecPlan 骨架模板

    # <简短的、面向行动的描述>

    本 ExecPlan 是一份活文档。"进度"、"意外发现"、"决策日志"和"成果与复盘"章节必须随着工作推进持续更新。

    如果 PLANS.md 文件已提交到仓库，在此处注明该文件从仓库根目录算起的路径，并说明本文档必须按照 PLANS.md 的要求来维护。

    ## 目的 / 全局视角

    用几句话解释这个改动完成后用户能获得什么，以及如何看到它在工作。描述你将要实现的用户可见行为。

    ## 进度

    用带复选框的列表来总结细粒度的步骤。每次停顿都必须记录在这里，即使需要把一个部分完成的任务拆成两条（"已完成"和"剩余"）。本章节必须始终反映工作的实际当前状态。

    - [x] (2025-10-01 13:00Z) 示例：已完成的步骤。
    - [ ] 示例：未完成的步骤。
    - [ ] 示例：部分完成的步骤（已完成：X；剩余：Y）。

    使用时间戳来衡量进度速率。

    ## 意外发现

    记录实现过程中发现的意外行为、bug、优化点或洞察。提供简洁的证据。

    - 观察：……
      证据：……

    ## 决策日志

    按以下格式记录工作过程中做出的每个决策：

    - 决策：……
      理由：……
      日期/作者：……

    ## 成果与复盘

    在重要里程碑完成时或整个计划完成时，总结成果、差距和经验教训。将结果与最初的目的进行对比。

    ## 上下文与导航

    像读者什么都不知道一样，描述与本任务相关的当前状态。用完整路径命名关键文件和模块。定义你将使用的所有非显而易见的术语。不要引用之前的计划。

    ## 工作计划

    用散文体描述编辑和新增的顺序。对于每处编辑，指明文件和位置（函数、模块）以及要插入或修改的内容。保持具体和精简。

    ## 具体步骤

    说明要运行的确切命令以及在哪里运行（工作目录）。当命令产生输出时，展示一段简短的预期输出，方便读者对照。本章节必须随工作推进持续更新。

    ## 验证与验收

    描述如何启动或操作系统，以及应该观察到什么。用行为来表述验收标准，包含具体的输入和输出。如果涉及测试，写明"运行 <项目的测试命令>，预期 <N> 个通过；新测试 <名称> 在改动前失败、改动后通过"。

    ## 幂等性与恢复

    如果步骤可以安全地重复运行，说明这一点。如果某个步骤有风险，提供安全的重试或回滚路径。完成后保持环境干净。

    ## 产出物与备注

    把最重要的输出记录、diff 或代码片段作为缩进示例包含进来。保持简洁，聚焦于能证明成功的内容。

    ## 接口与依赖

    要具体明确。指明要使用的库、模块和服务，以及原因。指定在里程碑结束时必须存在的类型、trait/接口和函数签名。优先使用稳定的名称和路径，如 `crate::module::function` 或 `package.submodule.Interface`。例如：

    在 crates/foo/planner.rs 中，定义：

        pub trait Planner {
            fn plan(&self, observed: &Observed) -> Vec<Action>;
        }

如果你遵循以上指南，一个无状态的智能体——或者一个人类新手——就能从头到尾读完你的 ExecPlan，并产出一个可运行的、可观察的结果。这就是标准：自包含、自给自足、面向新手、聚焦成果。

当你修订计划时，必须确保你的改动全面反映在所有章节中，包括活文档章节，并且必须在计划末尾写一条说明，描述改动内容和原因。ExecPlan 不仅要描述"做什么"，还要描述几乎所有事情的"为什么"。
~~~
