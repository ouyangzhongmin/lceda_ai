# 嘉立创 EDA AI 助手插件技术架构文档

## 1. 文档信息
- 文档版本：v0.2
- 创建日期：2026-03-24
- 关联文档：`需求文档.md`

## 2. 架构目标
本项目采用“本地插件 Agent + 云端 Go 服务”的双层架构。

目标如下：
- 在嘉立创 EDA 标准版与专业版插件内实现原理图上下文获取、交互式分析、问题定位、AI 绘制预览与本地规则执行。
- 在独立 Go 服务端实现 RAG 检索、LLM 代理转发、用户账户体系、Credits 计费与消费流水。
- 同时支持两种 LLM 接入模式：
  - 用户自定义 LLM 配置直连模式。
  - 通过自有 Go 服务代理调用并按 Credits 扣费模式。
- 保证原理图敏感数据可控传输，默认最小化上传，重要操作可审计。

## 3. 总体架构

### 3.1 架构分层
- 嘉立创 EDA 本地插件层：
  - 负责 UI、原理图数据读取、对象定位、轻量规则校验、生成结果预览与用户确认。
- 本地 Agent 运行层：
  - 作为插件内的任务编排核心，组织上下文构建、规则引擎执行、RAG 请求、LLM 调用、结果回填。
- Go 服务端：
  - 提供认证、账户、Credits、RAG 检索、LLM 代理、审计日志、配置策略等能力。
- 数据与知识层：
  - 存储知识库、向量索引、元件知识、设计规范、用户账户、Credits 账本、调用日志。

### 3.2 推荐部署拓扑
- 客户端：
  - 嘉立创 EDA 标准版与专业版插件，运行在用户本机的 EDA 环境中。
- 服务端：
  - Go 单体服务优先，后续按模块拆分，gin + wire + gorm,目录采用整洁架构 。
- 数据层：
  - `PostgreSQL`：账户、订单、Credits 账本、调用记录、知识条目元数据。
  - `Qdrant`：RAG 向量检索、相似度召回、按元数据过滤。
  - `Redis`：会话、验证码、限流、短期缓存。
  - 对象存储：知识源文档、手册、分块文件、导出报告。

### 3.3 Qdrant 部署与索引策略
- 部署方式：
  - 首版采用独立 `Qdrant` 实例部署，与 `PostgreSQL` 解耦。
- 集合设计：
  - `electronics_principles`：电子原理知识片段。
  - `component_knowledge`：元件知识与典型应用片段。
  - `design_rules`：公开设计规范、审核标准片段。
- 向量配置建议：
  - 每个 collection 统一使用同一 embedding 维度。
  - 距离函数优先使用 `Cosine`。
  - 首版采用单向量字段，后续再扩展多向量策略。
- 分片与副本：
  - 开发环境单分片单副本。
  - 生产环境根据数据量与可用性要求调整分片、副本和快照策略。

## 4. 核心技术路线

### 4.1 本地插件端
- 开发标准：
  - 按嘉立创标准版与专业版插件/扩展标准开发，遵循官方 `SDK + extension.json + build/dist/.eext` 打包流程。
- 智能体实现约束：
  - 插件内智能体遵循 `ReAct` 模式进行任务编排。
  - 智能体必须通过 `Tool Calling` 调用编辑器能力、规则引擎、RAG、绘图草案生成等外部能力。
  - 智能体需支持 `MCP` 方式接入外部资源、结构化数据和工具服务。
  - 智能体需支持 `Skill` 机制，用于按任务动态装载领域能力。
- 技术栈建议：
  - `TypeScript`
  - 插件官方 SDK / API Types
  - 轻量状态管理
  - 本地规则引擎模块
- 核心模块：
  - `editor-adapter`：封装嘉立创编辑器 API。
  - `schematic-context-builder`：抽取元件、网络、引脚、属性、选区、跨页关系。
  - `rule-engine`：接线错误、电气冲突、接反接错、属性校验。
  - `agent-orchestrator`：统一调度本地规则、RAG、LLM、绘图建议。
  - `tool-registry`：统一注册和调度本地工具、服务端工具及 MCP 工具。
  - `mcp-client`：负责发现、连接和调用 MCP 资源与工具。
  - `skill-loader`：负责技能清单加载、启停控制和上下文注入。
  - `llm-provider-manager`：管理自定义 LLM 和服务端代理 LLM 两类模式。
  - `auth-session`：管理登录态、token、Credits 余额展示。
  - `ui-panel`：聊天、分析结果、引用证据、绘图预览、错误定位。

### 4.1.1 Agent 实现模式
- `ReAct` 编排：
  - 将用户任务拆分为“思考 -> 调用工具 -> 观察结果 -> 继续决策”的循环。
  - 适用于原理图分析、接线错误解释、局部修复建议、绘图草案生成。
- `Tool Calling` 范围：
  - 编辑器上下文工具
  - 规则检测工具
  - RAG 检索工具
  - 元件知识查询工具
  - 图形对象计划生成工具
  - 登录与账户状态查询工具
- `MCP` 接入定位：
  - 作为外部知识、资源、工具的统一接入层。
  - 优先用于知识库查询、结构化数据访问和可扩展工具接入。
- `Skill` 机制定位：
  - 将领域能力封装为独立技能包。
  - 典型技能包括：
    - 原理图分析技能
    - 元件知识解释技能
    - 接线标准检查技能
    - 电源模块绘图技能
    - 设计规范审查技能
- 结果约束：
  - 高风险输出必须能回溯到工具结果、规则命中或 RAG 引用。
  - 不允许仅依赖模型自由生成给出关键工程结论。

### 4.2 Go 服务端
- 开发语言：
  - `Go`
- 服务职责：
  - 提供统一 HTTPS API。
  - 对接多个 LLM 供应商。
  - 提供 RAG 检索与重排。
  - 管理邮箱注册登录、微信扫码登录、token 签发、用户资料、Credits 扣费。
  - 记录调用日志与账本流水。
- 核心模块：
  - `api-gateway`：统一鉴权、路由、限流、审计入口。
  - `auth-service`：邮箱注册、验证码登录、微信登录接入、登录会话管理、刷新 token、注销。
  - `credits-service`：余额、充值、消费、冻结、退款、账本。
  - `llm-proxy-service`：模型路由、参数校验、响应流式转发、成本统计。
  - `rag-service`：查询改写、检索、重排、引用拼装。
  - `knowledge-service`：知识导入、切片、embedding、版本管理。
  - `policy-service`：模型可用性、价格策略、限额、风控策略。

### 4.2.1 Go 服务端微信登录支持
- 接入目标：
  - Go 服务端统一承接微信开放平台网站应用扫码登录流程。
- 服务端职责：
  - 生成微信登录跳转地址。
  - 管理 `state`、`login_session_id`、一次性登录票据。
  - 接收微信 OAuth 回调并校验授权结果。
  - 获取微信侧用户标识，如 `openid`、`unionid`。
  - 完成用户查找、创建、账号绑定和 token 签发。
- 账号体系：
  - 同一平台用户支持绑定多个登录凭证：
    - 邮箱
    - 微信 `unionid/openid`
  - 首版建议优先按 `unionid` 做微信账户主识别键，`openid` 作为应用侧辅助标识。
- 统一登录出口：
  - 无论是邮箱验证码登录还是微信扫码登录，最终都写入同一套登录会话状态，并由插件使用相同接口换取 token。
- 风险控制：
  - 微信回调必须校验 `state`。
  - 登录票据必须一次性使用并设置短期过期时间。
  - 账号绑定和自动注册流程需保留审计日志。

### 4.3 知识库与 RAG
- 知识库分层：
  - 电子原理知识库：拓扑、接口规范、电源设计、保护电路、常见错误模式。
  - 元件知识库：器件参数、推荐接法、典型应用、极性说明、关键脚定义。
  - 设计规则知识库：公开设计规范、命名规范、评审规则。
- 首版范围约束：
  - 首版不接入企业私有规范知识库。
  - 首版仅面向通用电子原理知识与元件知识增强。
- Qdrant payload 设计：
  - `doc_id`：原始文档 ID。
  - `chunk_id`：切片 ID。
  - `kb_type`：知识库类型，如 `principle`、`component`、`rule`。
  - `title`：文档或条目标题。
  - `tags`：主题标签、器件类别、接口类型。
  - `component_type`：器件类别，如 `LDO`、`MCU`、`USB`、`ESD`。
  - `voltage_level`：适用电压等级。
  - `package`：封装信息。
  - `source_type`：来源类型，如手册、规范、内部知识。
  - `source_ref`：来源引用标识，如文档号、版本号、页码。
  - `lang`：语言。
  - `tenant_id`：租户或项目隔离标识。
  - `is_active`：是否生效。
  - `updated_at`：更新时间。
- RAG 流程：
  - Query 规范化。
  - 结合原理图上下文做检索条件增强。
  - 多路召回：关键词检索 + 向量检索。
  - 重排与证据筛选。
  - 生成引用片段。
  - 将证据和原理图上下文一起发送到 LLM。
- Qdrant 检索策略：
  - 先根据任务类型选择 collection。
  - 再根据原理图上下文拼接 metadata filter，例如器件类型、电压等级、知识库范围。
  - 执行 Top-K 向量召回。
  - 对召回结果做去重、邻近 chunk 合并与重排。
  - 输出最终证据片段及引用元数据。
- Qdrant 过滤示例：
  - 元件接反检查时，优先过滤 `component_type`、`package`、`kb_type=component`。
  - 电源设计问答时，优先过滤 `kb_type=principle`、`voltage_level`、`tags`。
  - 企业规则审查时，优先过滤 `kb_type=rule`、`tenant_id`、`is_active=true`。
- 结果约束：
  - 输出必须带证据来源。
  - 元件参数类回答优先使用知识库事实，禁止无依据补全。

### 4.4 Go 侧 Qdrant 封装设计
- 基础封装：
  - `VectorStore` 接口屏蔽底层向量库实现，首版实现为 `QdrantVectorStore`。
- 建议接口：
  - `UpsertChunks(ctx, collection, chunks)`
  - `Search(ctx, collection, vector, filter, topK)`
  - `DeleteByDocID(ctx, collection, docID)`
  - `CreateCollectionIfNotExists(ctx, schema)`
 - 检索链路：
  - `knowledge-service` 负责切片、embedding、upsert 到 `Qdrant`。
  - `rag-service` 负责构造 filter、执行 search、重排和证据拼装。
- 工程约束：
  - 不在业务层直接拼接 `Qdrant` 查询细节。
  - `Qdrant` collection 名称、向量维度、距离函数由配置中心统一管理。
  - 所有写入操作需记录文档版本与来源，便于后续重建索引。

## 5. 关键业务流程

### 5.1 原理图分析流程
1. 插件读取当前原理图或选中模块数据。
2. `agent-orchestrator` 基于 `ReAct` 选择技能并构建执行计划。
3. 本地构建结构化上下文并执行首轮规则检查。
4. 若用户发起 AI 分析，则通过工具调用向 Go 服务发送裁剪后的结构化上下文。
5. Go 服务执行 RAG 检索，拼装证据。
6. LLM 代理生成分析结果与建议。
7. 插件展示问题列表、证据引用和定位入口。

### 5.2 AI 绘制原理图流程
1. 用户输入自然语言需求。
2. `agent-orchestrator` 根据任务选择绘图技能与工具链。
3. 插件将当前页面上下文、设计约束、可用器件信息发送给 Agent。
4. Go 服务执行 RAG，补充典型电路与元件接法知识。
5. LLM 输出结构化电路草案。
6. 插件先展示器件清单、连接关系、关键网络和主要属性建议。
7. 用户确认草案后，插件本地将草案转换为图形对象计划。
8. 本地规则引擎执行生成前校验。
9. UI 展示最终预览，确认后调用编辑器 API 落图。

### 5.3 Credits 消费流程
1. 用户选择“通过服务端 Credits 模式使用”。
2. 插件要求用户先完成邮箱注册/登录。
3. 登录成功后获取 `access token` 与用户账户信息。
4. 每次 RAG/LLM 调用前先进行额度检查或服务端侧预扣。
5. 调用完成后按模型、token、功能类型进行实际扣费。
6. 插件展示余额、消费明细、失败原因。

### 5.4 浏览器登录与插件状态同步流程
1. 插件向 Go 服务申请 `login_session_id`。
2. Go 服务返回：
   - `login_url`
   - `login_session_id`
   - `poll_token`
   - `expires_at`
3. 插件调用宿主能力打开系统默认浏览器访问 `login_url`。
4. 用户在浏览器完成登录：
   - 邮箱验证码登录。
   - 或微信扫码登录。
5. Go 服务在登录成功后将 `login_session_id` 状态写入 `Redis`。
6. 插件通过登录状态接口进行短轮询或长轮询。
7. 插件检测到状态为 `success` 后，使用一次性票据换取正式 `access token` 和 `refresh token`。
8. 插件停止轮询，更新登录态和 Credits 信息。

### 5.5 自定义 LLM 模式流程
1. 用户在插件内配置模型供应商、Base URL、API Key、模型名。
2. 插件将该配置保存在本地配置区，不上传到服务端。
3. 分析或生成任务直接由插件请求用户指定模型。
4. 若用户启用 RAG，可选择：
   - 仅使用本地规则，不使用服务端 RAG。
   - 仅使用服务端 RAG，但生成走自定义 LLM。
   - 全部走服务端代理。

## 6. 模块架构设计

### 6.1 插件端模块划分
- `UI Layer`
  - 聊天面板
  - 问题列表
  - 引用证据面板
  - 登录与 Credits 面板
  - LLM 配置面板
- `Application Layer`
  - Agent 编排
  - Tool 调度
  - MCP 接入管理
  - Skill 管理
  - 分析任务管理
  - 生成任务管理
  - 会话管理
- `Domain Layer`
  - 原理图实体模型
  - 规则模型
  - 生成草案模型
  - 引用证据模型
- `Infrastructure Layer`
  - 嘉立创 API 适配器
  - HTTP Client
  - 本地存储
  - 日志与错误上报

### 6.2 服务端模块划分
- `Auth`
  - 邮箱注册
  - 邮件验证码登录
  - 微信扫码登录
  - 微信账号绑定
  - JWT/Session 管理
- `Credits`
  - 余额查询
  - 扣费引擎
  - 消费账本
  - 套餐与价格规则
- `RAG`
  - 文档导入
  - 切片
  - embedding
  - 检索与重排
- `LLM Proxy`
  - 多供应商适配
  - 模型映射
  - token 统计
  - 失败重试
- `Ops`
  - 审计日志
  - 限流
  - 配置中心
  - 告警

## 7. 数据流设计

### 7.1 插件到服务端的数据边界
- 默认上传内容：
  - 当前任务相关的结构化原理图上下文。
  - 选中器件、网络、引脚及属性。
  - 用户输入问题或生成需求。
- 默认不上传内容：
  - 完整工程历史版本。
  - 与当前分析无关的整库数据。
  - 用户显式禁止上传的页面或字段。

### 7.2 建议的结构化上下文格式
- 工程信息：
  - 工程 ID、页面 ID、页名、版本号。
- 元件信息：
  - 位号、器件名、封装、值、库 ID、关键属性。
- 引脚信息：
  - 引脚号、引脚名、电气类型、所属器件。
- 网络信息：
  - 网络名、连接点、驱动关系、是否电源网。
- 规则信息：
  - 当前命中的问题、严重级别、规则 ID。

### 7.3 生成结果格式
- 文本结论：
  - 问题说明、风险说明、修改建议。
- 引用证据：
  - 知识条目 ID、标题、版本、片段摘要。
- 绘图草案：
  - 器件清单
  - 连接清单
  - 网络命名
  - 属性建议
  - 落图坐标建议

## 8. 认证与 Credits 设计

### 8.1 邮箱注册登录
- 支持模式：
  - 邮箱验证码注册/登录。
  - 邮箱 + 密码注册后验证码登录。
- 推荐方案：
  - 首版使用邮箱验证码登录，减少密码找回和存储复杂度。
- 首版登录入口：
  - 采用“插件打开浏览器登录页，服务端维护登录会话，插件轮询登录状态”的方案。
- 令牌机制：
  - 短期 `access token`
  - 长期 `refresh token`
- 插件能力：
  - 登录弹窗
  - 打开浏览器登录页
  - 账户信息查看
  - 登出
  - 会话过期自动刷新

### 8.2 微信扫码登录设计约束
- 微信扫码登录不作为首版唯一登录方式。
- 首版主登录方式仍为邮箱验证码登录。
- 微信扫码登录接入前提：
  - 使用微信开放平台“网站应用微信登录”能力。
  - 不将个人公众号 `AppID` 视为可直接替代的网站扫码登录 `AppID`。
  - 完成开放平台资质、域名、回调地址及审核要求确认。
- 工程策略：
  - 浏览器页承载微信扫码流程。
  - 插件不内嵌复杂微信登录页面。
  - 登录成功后仍复用统一的 `login_session_id` 状态同步机制。

### 8.3 登录状态同步策略
- 首版推荐：
  - `Redis + 插件短轮询`
- 轮询策略：
  - 前 30 秒每 `2-3 秒` 轮询一次。
  - 超过 30 秒后退避到每 `5 秒` 一次。
  - 登录成功、失败、过期、用户取消后立即停止轮询。
- 状态存储：
  - 登录会话状态放在 `Redis`，不直接查询 `PostgreSQL`。
  - 每个 `login_session_id` 设置 `TTL`，建议 `5 分钟`。
- 返回数据：
  - 状态查询接口只返回轻量状态，如 `pending`、`success`、`expired`、`failed`。
- 演进策略：
  - 首版使用短轮询。
  - 第二阶段可升级为长轮询或 `SSE/WebSocket` 服务端推送。

### 8.4 不采用 localhost 回调作为首版主路径
- 不将 `localhost callback` 作为首版默认方案。
- 原因：
  - 嘉立创插件环境是否支持稳定启动本地监听服务尚未验证。
  - 插件生命周期、端口占用、防火墙、跨平台差异都会增加不确定性。
  - 相比之下，浏览器登录 + 服务端会话 + 插件轮询对宿主环境要求更低。
- 可选预研方向：
  - 若后续验证嘉立创插件可稳定起本地回调服务，可将 `localhost callback` 作为增强方案引入。

### 8.5 Credits 计费模型
- 计费维度建议：
  - 按模型调用计费
  - 按 token 计费
  - 按功能套餐计费
- 首版建议：
  - 采用“功能类型 + token 用量”的混合计费。
- 账本要求：
  - 每次扣费均生成不可变流水。
  - 支持预扣和结算修正。
  - 支持失败回滚。

### 8.6 模式切换策略
- `Direct Mode`
  - 用户自定义 LLM 配置，不消费平台 Credits。
- `Credits Mode`
  - 调用自有 Go 服务代理，必须登录，按 Credits 扣费。
- 插件内应允许用户在两种模式间显式切换。
- 用户体系边界：
  - 首版仅支持个人用户。
  - 团队、组织、租户隔离能力不纳入首版实现，但数据库和服务接口需预留后续扩展字段。

## 9. LLM 代理设计

### 9.1 为什么要做代理层
- 屏蔽不同 LLM 厂商接口差异。
- 统一计费与风控。
- 实现模型路由和故障切换。
- 为 RAG 与审计提供统一入口。

### 9.2 代理能力
- 多模型支持：
  - OpenAI 兼容接口
  - 其他主流供应商
- 请求能力：
  - 普通对话
  - 流式响应
  - 结构化 JSON 输出
- 控制能力：
  - 最大 token
  - 温度上限
  - 敏感词和风控
  - 超时控制

## 10. 可行性评估

### 10.1 总体结论
该技术路线总体可行，且适合分阶段实现。

原因如下：
- 嘉立创官方文档已明确存在插件/扩展开发体系、SDK、`extension.json`、构建与导入流程。
- 插件端适合承担 UI、编辑器上下文获取、对象定位和本地规则执行。
- RAG、LLM 代理、Credits、认证等能力更适合放在独立 Go 服务端，避免插件端过重且便于统一运营。

### 10.2 可直接成立的部分
- 本地插件按嘉立创标准开发。
- 本地插件按标准版与专业版双端兼容方向设计。
- 插件调用远端 HTTP 服务。
- 插件内实现 `ReAct` 式智能体编排。
- 插件通过 `Tool Calling` 调用本地与服务端能力。
- 插件支持两种模型模式：
  - 用户自定义 LLM
  - 平台 Credits 模式
- Go 服务端实现 RAG、LLM 代理、用户登录、Credits。
- 浏览器承载登录流程，插件通过服务端会话状态获取登录结果。

### 10.3 需要优先验证的关键点
- 标准版与专业版插件 API 的差异：
  - 原理图对象访问能力
  - 插件 UI 能力
  - 程序化落图能力
- 嘉立创插件 API 是否能稳定获取：
  - 原理图完整 JSON/source
  - 当前页面/选区/器件/网络/属性
  - 程序化选中与定位
- 嘉立创插件 API 是否支持稳定执行：
  - 批量放置器件
  - 自动连线
  - 批量撤销/事务式应用
- 插件运行环境是否支持：
  - 安全保存自定义 API Key
  - 打开外部登录页或邮箱验证页
  - 长连接/流式响应
  - 足够稳定的本地持久化
- 嘉立创插件环境是否适合承载 `MCP client`、工具注册与技能动态装载。
- 微信开放平台资质是否满足网站扫码登录要求。

### 10.4 对“本地 Agent”的建议修正
“本地 Agent”应理解为“插件内任务编排器”，而不是在本机常驻独立进程。

建议原因：
- 嘉立创插件的主运行环境更像受限扩展运行时，不适合引入复杂本地守护进程作为首版依赖。
- 若首版要求用户额外安装本地服务，会显著提高交付与运维成本。
- 插件内编排 + 远端 Go 服务是更稳妥的 MVP 路线。

如后续确需更强本地能力，可在第二阶段增加可选本地 Companion Service。

## 11. 风险与约束
- 标准版与专业版插件 API 差异可能导致双端适配成本增加。
- 插件 API 能力边界不明确，可能影响“AI 自动落图”的完成度。
- 自定义 LLM 模式涉及 API Key 本地存储，需明确本地配置区的安全边界与泄露风险。
- 浏览器登录方案依赖插件可可靠打开外链，以及可稳定执行登录状态轮询或长轮询。
- 微信扫码登录受开放平台资质、回调域名和审核策略约束，首版不应作为唯一登录手段。
- Credits 扣费必须做幂等控制，否则会出现重复计费。
- RAG 质量高度依赖知识清洗与分块策略。

## 12. MVP 实施建议

### 12.1 Phase 1
- 完成插件 UI 外壳。
- 完成标准版与专业版双端最小兼容验证。
- 完成原理图上下文读取。
- 完成本地规则引擎。
- 完成 Go 服务端基础框架、邮箱验证码登录、Credits 账户、基础 LLM 代理。
- 完成浏览器登录页、`login_session_id` 会话机制和插件短轮询闭环。

### 12.2 Phase 2
- 接入 RAG 检索。
- 实现带引用的问答与分析。
- 实现 Credits 计费闭环。
- 评估并接入微信扫码登录。

### 12.3 Phase 3
- 实现 AI 绘图草案生成。
- 实现预览与确认。
- 在 API 能力允许范围内实现自动放置与连线。
- 视插件能力决定是否引入 `localhost callback` 或服务端推送优化登录体验。

## 13. 立项前验证清单
1. 用官方扩展 SDK 创建最小插件，验证可导入运行。
2. 分别验证标准版与专业版插件能否读取当前原理图 source / JSON。
3. 分别验证标准版与专业版插件能否定位元件、网络和选区。
4. 分别验证标准版与专业版插件能否创建器件、连线或修改原理图对象。
5. 验证插件能否访问外部 HTTPS 服务并支持流式响应。
6. 验证插件能否保存本地配置与登录态。
7. 验证插件能否打开系统浏览器并完成邮箱验证码登录闭环。
8. 验证插件短轮询登录状态接口的稳定性与宿主兼容性。
9. 验证微信开放平台网站应用资质与回调要求。
10. 验证 `MCP client`、技能装载和工具注册在双端环境下的可行性。

## 14. 结论
推荐采用以下正式架构：

- 插件端：
  - 嘉立创标准版与专业版扩展
  - 内置 Agent 编排器
  - 本地规则引擎
  - 自定义 LLM 配置
  - 登录与 Credits UI
- 服务端：
  - Go 单体服务起步
  - RAG 检索
  - LLM 代理
  - 邮箱认证
  - Credits 账本

该方案兼顾可落地性、演进性与商业化能力，前提是先完成“插件 API 能力验证”这一前置工作。
