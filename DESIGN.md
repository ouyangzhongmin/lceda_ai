# DESIGN.md

## 作用
- 定义项目级设计原则与不可妥协的工程约束。
- 不重复记录已经在需求、接口、数据库或任务计划中明确的细节。

## 产品边界
- 产品定位为嘉立创 EDA 原理图场景下的 AI 助手插件。
- P0 必须同时支持标准版与专业版。
- P0 仅面向个人用户。
- P0 不接入企业私有知识库。

## 安全优先
- 高风险工程结论不能只依赖模型自由生成。
- 原理图修改必须遵循 `生成 -> 校验 -> 确认 -> 应用`。
- 首版允许先生成草案，但不允许无确认自动落图。
- 规则引擎证据优先于模型猜测。

## Agent 原则
- 插件智能体必须遵循 `ReAct`。
- 智能体必须通过 `Tool Calling` 调用编辑器能力、规则检查、RAG 与草案规划能力。
- 智能体必须支持 `MCP` 作为结构化扩展能力层。
- 智能体必须支持 `Skill` 装载领域工作流。
- 智能体输出必须可通过工具轨迹、规则命中或引用证据追溯。

## 架构原则
- 轻编排留在插件端，重能力放在 Go 服务端。
- 优先保证服务边界清晰，而不是过早引入本地复杂度。
- 登录默认采用浏览器登录 + 服务端会话状态同步。
- 在插件运行时能力未验证前，不将 `localhost callback` 作为主路径依赖。

## 兼容性原则
- 标准版与专业版插件运行环境都是一等目标。
- 任何依赖编辑器 API 的能力都必须评估双端差异。
- 当平台能力不一致时，P0 优先采用更安全的交集能力。

## 编码约束
- 新增或修改的代码注释统一使用中文。
- 注释只写必要信息，避免重复描述显而易见的代码行为。

## 数据与隐私
- 仅上传与当前任务相关的原理图上下文。
- 自定义 LLM API Key 仅保存在插件本地，不上传服务端。
- 知识检索结果必须可追溯到明确来源。

## 演进策略
- 用最小但可扩展的方式构建以下基础能力：
  - 登录提供方
  - Credits 计费
  - Agent Skill
  - MCP 工具
  - 后续团队支持
- 不为了抽象而抽象，只有在确实降低后续迁移成本时才增加层次。

## 决策优先级
- 当多个取舍冲突时，优先级如下：
  1. 安全性
  2. 双端兼容性
  3. 可解释性
  4. 交付速度
  5. 使用便利性
