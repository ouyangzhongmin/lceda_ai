# 当前执行跟踪

## 1. 目标
- 持续追踪“可跑通全流程”目标下的完成度、阻塞项与下一步执行顺序。

## 2. 当前完成度（2026-03-25）
- 已完成：
  - 服务端认证闭环（邮箱登录、token 交换/刷新、登出、`users/me`）。
  - 微信登录 PoC 闭环与 OAuth 参数化接入骨架。
  - Credits 余额/流水与 LLM 调用扣费。
  - RAG 检索、引用包（含 `citation_lines`）与审计落盘/落库回退。
  - 插件侧 ReAct + ToolRegistry + 规则分析 + 定位 + 草案预览/应用骨架。
  - 插件侧 Skill + MCP 最小可运行链路：
    - Skill 选择与白名单工具约束
    - MCP 资源发现与 MCP 工具注册
  - 知识文档导入流程 PoC：
    - `POST /api/v1/knowledge/documents`
    - `GET /api/v1/knowledge/documents`
    - `POST /api/v1/knowledge/documents/{document_id}:reindex`
  - 知识导入任务化 PoC：
    - `POST /api/v1/knowledge/import-tasks`
    - `GET /api/v1/knowledge/import-tasks/{task_id}`
    - `POST /api/v1/knowledge/import-tasks/{task_id}:run`
    - `POST /api/v1/knowledge/import-tasks/{task_id}:enqueue`
  - 知识导入任务后台队列 + 自动重试（PoC 工程化版本）
  - 草案分支接入 LLM 生成提示（用于 draft plan 前置增强）
  - `editor.rollback_apply_plan` 与事务回滚 PoC：
    - source API 回滚
    - shape API 回滚
    - fake host 回滚
  - 知识导入任务队列工程化：
    - 可替换队列接口
    - 进程内队列实现
    - Redis 队列实现（Redis 可用时自动启用）
    - Redis Stream + Consumer Group 接入（含 ack）
    - 队列统计与死信记录接口（`stats` / `dead-letters`）
    - 队列参数化配置（queue mode/stream/group/block/claim/lock ttl）
    - stream 运行时观测指标（ack、claimed/new、pending）
    - stream claim 重投策略参数化（enabled/count/interval/min-idle）
    - ack 重试策略参数化（retry count/interval）与重试观测（retry/retry exhausted）
    - 死信任务重试恢复接口（`POST /knowledge/import-tasks/{task_id}:retry`）
    - 任务创建幂等去重（支持 `idempotency_key`，含自动去重键）
    - 同任务并发防重处理（避免重复消费导致多次执行）
    - Redis 分布式任务锁（跨实例同任务防重复执行）
- 已验证：
  - `server` 全量测试通过：`go test ./...`
  - 插件端主 PoC 链路通过：`POC_USE_FAKE_HOST=1 npm run poc:plugin`

## 3. 当前阻塞项
- 真实嘉立创标准版/专业版宿主环境验证尚未完成。
- 真实宿主浏览器拉起能力尚未完成实测。
- 真实落图（apply-plan）与撤销回退在真实宿主环境尚未完成实测。
- 微信开放平台生产配置与回调域名联调尚未完成。
- 知识导入任务已支持 Redis Stream + Consumer Group 与死信记录；仍缺生产级消息系统能力（多副本、高可用、重投策略细化）。

## 4. 下一步（优先级）
1. 收口登录主线：长轮询、真实宿主浏览器登录、微信开放平台生产联调。
2. 收口插件端双端真实宿主能力：读取/定位/落图闭环与失败回退策略。
3. 收口 LLM 透传主线：超时、重试、错误码、Credits 计费一致性。
4. 在以上主线稳定后，再推进知识库生产级入库管道增强。
5. 收口发布前文档（安装说明、已知限制、验收清单）。

## 4.1 本轮新增完成
- 主线优先级已调整为：
  - 登录与账户闭环
  - 插件双端真实宿主能力
  - LLM 透传与 Credits 稳定性
- 登录轮询链路增强：
  - 服务端 `GET /auth/login-sessions/{id}` 支持 `wait_seconds` 长轮询
  - 插件侧登录轮询改为优先使用长轮询参数
- 登录页面收口：
  - 服务端提供 `/login` 页面用于浏览器登录
  - `login_url` 包含 `session` 与 `poll_token`
  - 微信回调支持 HTML 重定向至 `next_url`
- LLM 透传稳定性增强：
  - OpenAI 兼容 provider 支持可配置超时与重试（timeout/retry/backoff）
  - `llm.generate` 增加 `messages` 非空校验
- Plugin host capability validation:
  - Validate host capability report before context fetch; missing required capabilities block analysis and surface errors in UI/logs.
  - Added missing-host validation script: `npm run poc:missing-host`.
  - Added real-host probe script: `npm run poc:host-probe`.
  - Aligned packaging path with official tutorial: `npm run build` -> `plugin/build/dist/*.eext`.
  - Added `.edaignore` and `build/package` staging directory for extension packaging.
  - Migrated plugin engineering shell toward official `pro-api-sdk` style:
    - `config/esbuild.common.ts`
    - `config/esbuild.prod.ts`
    - `build/packaged.ts`
    - `@jlceda/pro-api-types`
  - Added typed host runtime probe based on `@jlceda/pro-api-types` to inspect current document and selected primitives in real host.
  - Standard/professional host bridge now prefers typed host APIs for selection read and external window open before falling back to legacy runtime guesses.
  - Current document / schematic page metadata now also prefers typed host APIs (`dmt_SelectControl`, `dmt_Schematic`) to fill project/page context.
  - Standard/professional raw host resolver now supports pure official typed runtime startup even when `lc` / `lcPro` / legacy `api(...)` are absent.
  - Locate path now prefers official typed APIs:
    - `eda.sch_SelectControl.doSelectPrimitives(...)`
    - `eda.sch_Primitive.getPrimitivesBBox(...)`
    - `eda.sch_Document.navigateToRegion(...)`
  - 插件激活阶段增加统一真实宿主自检日志输出：
    - `[lceda-ai-assistant] activate`
    - `[lceda-ai-assistant] capability_report`
    - `[lceda-ai-assistant] typed_host_probe`
    - `[lceda-ai-assistant] typed_document_context`
  - 插件激活后若宿主支持 `eda.sys_Dialog`，会直接弹出摘要对话框，显示当前通道、原理图统计、问题数、定位状态与 capability 状态。
  - 插件 UI 已从简陋的纯文本消息框升级为宿主内嵌 `IFrame` 面板骨架：
    - 顶部摘要区
    - 原理图统计卡片
    - 问题列表
    - 当前状态
    - 下一步建议
    - `sys_IFrame` 不可用时回退到 `sys_Dialog`
  - 面板已接入首批动作骨架：
    - `重新分析`
    - `登录`
    - 问题 `定位`
    - `生成草案`
  - 登录动作已升级为“浏览器登录 + 会话长轮询 + token 交换 + 面板自动回写登录态/Credits”闭环。
  - 面板增加状态同步轮询，用于承接后台登录状态变化与结果展示更新。
  - 聊天输入已接入 `sendChat`：根据输入内容自动切换分析/草案路径。
  - 草案闭环补充：
    - `应用草案`（`apply-plan`）
    - `回滚应用`（`rollback`）
  - 设置面板已支持自定义 LLM 可编辑保存，并回写到运行态显示。
  - 宿主内嵌面板视觉层已从工程调试态升级为更完整的产品化工作台风格：
    - 更强的层级与信息分区
    - 更清晰的统计卡片
    - 更适合工程场景的暖色玻璃化视觉
  - `plugin/README.md` 已收口真实嘉立创导入后的最短验证路径与控制台日志检查项。
  - 修复扩展打包策略：
    - `.eext` 仅包含最小运行集，避免将 `src/**/*.ts`、`package/`、`release/`、`.env.*` 等非运行文件打入扩展包，降低宿主加载阶段误解析源码导致的 `Unexpected token 'export'` 风险。
  - Verified official plugin engineering shell and typed host bridge changes still pass:
    - `npx tsc --noEmit`
    - `npm run build`
- 知识库导入已支持增量 upsert 与去重跳过：
  - 来源键：`kb_type + source_type + source_ref + lang`
  - 内容哈希一致时返回 `import_mode=skipped_duplicate`
  - 内容变化时返回 `import_mode=updated`
- 认证 token 路由统一为：
  - `POST /api/v1/auth/tokens:action?action=tokens:exchange`
  - `POST /api/v1/auth/tokens:action?action=tokens:refresh`
- 知识导入队列补充 ack 重试策略参数：
  - `KNOWLEDGE_ACK_RETRY_COUNT`
  - `KNOWLEDGE_ACK_RETRY_INTERVAL_MS`
- 知识导入队列补充任务执行重试策略参数：
  - `KNOWLEDGE_TASK_MAX_ATTEMPTS`
  - `KNOWLEDGE_TASK_RETRY_DELAY_MS`
  - `KNOWLEDGE_TASK_RETRY_BACKOFF_MODE`
  - `KNOWLEDGE_TASK_RETRY_MAX_DELAY_MS`
- 知识导入任务失败重试支持 fixed/exponential 退避，且修复了退避计算中的锁重入死锁。
- 知识导入队列 `stats` 补充重投治理观测：
  - `retry_delay_last_ms`
  - `retry_delay_total_ms`
  - `retry_backoff_fixed_count`
  - `retry_backoff_exponential_count`
  - `manual_retry_rejected_count`
- 知识导入任务 `:retry` 语义收口为仅死信任务可执行，非死信返回 `409001`。
- 知识导入队列统计补充 ack 重试观测：
  - `ack_retry_count`
  - `ack_retry_exhausted_count`

## 4.2 主线偏离检查（2026-03-26）
- 结论：未偏离当前 plan 主线。
- 依据：本轮改动围绕“插件双端真实宿主能力收口”，重点补齐官方 `pro-api-sdk` / `pro-api-types` 运行时下的 bridge 启动与定位链路，属于当前优先级第 2 项，未引入与主线无关的分支。

## 4.3 本轮新增完成（2026-04-20）
- 草案补全主线从“纯 LLM 生成”推进为“规则骨架 + 模板补全 + LLM 组装”：
  - 插件侧已支持消费 `external_rag_template_corpus`。
  - `draft_generate_plan` 支持显式传入外部模板语料，也可从 `rag search` 响应自动采用。
  - `normalizeDraftPlan` / `completeDraftPlanPeripherals` 已打通外部模板参与补全。
- Devboard 模板补全能力增强：
  - 本地模板库扩展并支持导入/导出结构化模板语料。
  - 模板召回支持“本地 + 外部语料”双源合并。
  - 低置信度模板保留建议态，不强制自动应用。
  - 预览侧可显示模板应用/建议摘要，并可区分来源（本地种子/外部开源语料）。
- 服务端 `/api/v1/rag/search` 链路已支持返回结构化模板语料：
  - handler 可透传 `external_rag_template_corpus`。
  - retriever/service/provider 已打通可选外部模板语料能力。
  - RAGFlow provider 已支持解析并暴露响应中的 `external_rag_template_corpus`。
- 服务端在“上游未返回结构化模板”时已具备 fallback 抽取能力（启发式）：
  - 已覆盖 `ESP32-S3 / ESP32-C3 / RP2040 / STM32F103` 的常见电源/按键/状态/下载模板。
  - 已覆盖 `USB-C 5V 输入` 防护组合（CC1/CC2、VBUS 保护等）。
  - 已覆盖外设小系统模板（I2C 传感器、MEMS 麦克风/I2S）作为结构化候选。
- 测试与回归：
  - 服务端相关测试通过：`internal/usecase/rag`、`internal/repository/ragflow`、`internal/transport/http/handlers`。
  - 插件关键回归通过：`draftTools`、`devboardRagCompletion`、`previewDraftPlan` 等相关测试集。
- 已完成“下一阶段主线”设计收敛并写入 spec：
  - 文档：`docs/superpowers/specs/2026-04-19-lceda-open-source-template-pipeline-design.md`
  - 结论：后续主线切换为“嘉立创开源原理图抓取（文件优先、文本兜底）+ 结构化模板抽取 + RAGFlow 模板库导入”，启发式规则仅保留 fallback 角色。

## 4.4 当前状态与交接点（切换工具前）
- 当前状态：
  - 已完成前后端模板语料消费链路与服务端基础模板输出能力。
  - 已完成下一阶段（嘉立创开源抽取管线）设计文档，尚未开始该管线脚本实现。
- 建议交接起点（下一工具/下一会话）：
  1. 基于 `2026-04-19-lceda-open-source-template-pipeline-design.md` 先落地 `lceda_open_source_crawler.py` 最小版本。
  2. 再实现 `extract_lceda_templates.py`，输出 `external_rag_template_corpus` 兼容结构。
  3. 最后实现 `transform_lceda_templates_for_ragflow.py`，接入模板型独立 dataset 导入验证。

## 4.5 本轮新增完成（2026-04-20）
- 已落地嘉立创开源模板数据管线最小版本：
  - `scripts/server/lceda_open_source_crawler.py`
  - `scripts/server/extract_lceda_templates.py`
  - `scripts/server/transform_lceda_templates_for_ragflow.py`
- 抓取层已具备：
  - 开源项目链接抽取与去重
  - 项目 URL 规范化
  - 项目详情页文本提取
  - 原理图/工程文件链接优先识别
  - `file_first` / `text_fallback` 来源模式输出
- 模板抽取层已具备：
  - `ESP32` / `RP2040` / `STM32` 主控家族识别
  - `usb_power_input`
  - `mcu_power_core`
  - `mcu_boot_reset`
  - `i2c_sensor_subsystem`
  - `status_indicator`
  - 高低置信度来源区分：
    - `lceda_open_source_extract`
    - `text_heuristic_fallback`
- RAGFlow 转换层已具备：
  - 输出 `title/content/metadata`
  - 保留 `external_rag_template_corpus`
  - 保留模板类型、主控锚点、质量分、来源项目信息
- 文档已补充：
  - `scripts/server/README.md` 增加开源模板管线命令与推荐产物目录
  - `docs/superpowers/plans/2026-04-20-lceda-open-source-template-pipeline-implementation.md` 增加本轮实现计划
- 验证证据：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline`
  - 结果：`Ran 8 tests ... OK`
- 真实样本验证补充：
  - 已使用真实嘉立创项目页完成最小链路验证：
    - `https://oshwhub.com/BOOW/esp32-s3`
    - `https://oshwhub.com/embeddedboys/rp2040_minimal`
  - 抓取产物：
    - `results/lceda_open_source_raw/sample_projects.jsonl`
  - 模板产物：
    - `results/lceda_template_corpus/sample_templates.jsonl`
  - RAGFlow 转换产物：
    - `results/lceda_ragflow_import/sample_templates.jsonl`
  - dry-run 导入结果：
    - `python3 scripts/server/ragflow_importer.py --input results/lceda_ragflow_import/sample_templates.jsonl --base-url http://127.0.0.1:39380 --api-key dry-run-key --dataset-id lceda-template-dataset --dry-run`
    - 结果：`{"files": 1, "rows": 2, "success": 2, "fail": 0, "retry": 0}`
- 真实样本联调中修复的根因问题：
  - 发现：真实项目页路径常为 `/<author>/<slug>`，而非 `/project/...`
  - 旧问题：crawler 仅识别 `/project/`，会把详情页里的 `project/choose` 误抓成项目记录
  - 已修复：
    - 屏蔽 `project/choose` / `project/new` 这类创建入口
    - 新增详情页特征识别（`工程详情` / `打开设计图` / `原理图` / `PCB`）
    - 允许将真实详情页直接作为项目输入源
  - 另已修复：
    - `RP2040_Minimal` 这类带下划线 slug 的主控识别
- 当前交接点：
  1. 补真实“分类页/搜索页 -> 项目列表”入口适配，当前仍主要依赖直接项目页样本验证。
  2. 补文件型输入解析，替代当前“文件已发现但仍主要依赖文本抽取”的过渡实现。
  3. 将模板类型扩展到 `usb_power_input` / `mcu_power_core` / `i2c_sensor_subsystem` 在真实样本上的稳定抽取。
  4. 用真实模板 dataset id 跑一次 RAGFlow API dry-run / 小批量导入验证。

## 4.6 本轮新增完成（2026-04-20）
- 分类页入口适配：
  - 已验证 `https://oshwhub.com/explore` 可发现真实项目链接。
  - 已支持嘉立创开源广场常见 `/<author>/<project_slug>` 项目路径，例如：
    - `https://oshwhub.com/li-chuang-kai-fa-ban/project_gzzvrwqn`
    - `https://oshwhub.com/htx-studio/project_kpilekig`
  - 已保留 `/project/...` 与 `/projects/...` 旧式路径兼容。
  - 已支持从 `application/json` 脚本里递归提取 `authorSlug + projectSlug` 形式的项目路径，作为搜索页/SSR 数据兜底。
- 入口噪声治理：
  - 继续屏蔽 `project/choose` / `project/new` 创建入口。
  - 修复“通用下载次数”误触发 `mcu_boot_reset` 的问题。
  - `/explore` 样本当前可抓到 2 个真实项目，但模板抽取为 0，符合当前低噪声策略。
- 模板抽取增强：
  - 对 MCU 开发板文本补充低置信度 `mcu_power_core` 模板。
  - 对 `USB转UART` / `CH340` / `CP210` / `自动下载` / `下载模式` 文本补充 `uart_download_header` 模板。
  - 真实项目样本：
    - `https://oshwhub.com/BOOW/esp32-s3`
    - `https://oshwhub.com/embeddedboys/rp2040_minimal`
  - 样本抽取结果：
    - `ESP32-S3 mcu_power_core`
    - `ESP32-S3 uart_download_header`
    - `RP2040 mcu_power_core`
- dry-run 导入验证：
  - `python3 scripts/server/ragflow_importer.py --input results/lceda_ragflow_import/sample_templates.jsonl --base-url http://127.0.0.1:39380 --api-key dry-run-key --dataset-id lceda-template-dataset --dry-run`
  - 结果：`{"files": 1, "rows": 3, "success": 3, "fail": 0, "retry": 0}`
- 当前交接点：
  1. 继续补真实搜索页入口：当前 `search?wd=ESP32` 返回工程数为 0，`search?wd=RP2040` 曾触发 WAF，仍需找到稳定关键词/分类 API。
  2. 补文件型输入解析与下载保存，提升 `file_first` 占比。
  3. 对真实分类页项目做批量质量评估，避免低置信度模板污染自动补全。

## 4.7 本轮新增完成（2026-04-20）
- 文件优先管线增强：
  - `lceda_open_source_crawler.py` 新增 `--file-output-dir`。
  - 抓取器识别到 `schematic_file_url` 后，会保存文件到项目 ID 子目录。
  - 项目记录会写入 `schematic_file_path`，供后续抽取器消费。
- 抽取器增强：
  - `extract_lceda_templates.py` 会读取 `schematic_file_path` 指向的文本型工程/原理图文件。
  - 文件内容会参与主控型号、USB-C、电源、UART 下载等模板识别。
  - 文件来源项目仍保持 `source_mode=file_first`，模板输出为：
    - `source=lceda_open_source_extract`
    - `quality_score=0.82`
- 本地 file-first 演练：
  - 输入：模拟 `ESP32-S3 + USB-C + CH340K + 3V3/GND/UART` 原理图 JSON。
  - 输出模板：
    - `usb_power_input`
    - `mcu_power_core`
    - `uart_download_header`
  - dry-run 导入结果：
    - `{"files": 1, "rows": 3, "success": 3, "fail": 0, "retry": 0}`
- 当前限制：
  - `.zip` 文件当前已支持文本型工程文件扫描，但尚未做多文件优先级裁剪。
  - 真实嘉立创项目详情页暂未稳定暴露可直接下载的原理图文件 URL。
  - 文件型解析目前是“文本型工程文件内容参与启发式识别”，还不是完整网表/器件图结构解析。
- 当前交接点：
  1. 继续寻找嘉立创真实工程文件下载 API 或详情页隐藏接口。
  2. 增加 zip 内部 `.json/.esch/.sch` 文件扫描和文本提取。
  3. 将文本型文件解析推进为结构化器件/网络抽取，降低启发式占比。

## 4.8 本轮新增完成（2026-04-20）
- zip 文件型输入解析：
  - `extract_lceda_templates.py` 已支持读取 `.zip` 内部文本型工程/原理图文件。
  - 当前扫描扩展名：
    - `.json`
    - `.epro`
    - `.esch`
    - `.sch`
  - 当前会忽略图片、预览图等二进制资源。
- zip file-first 演练：
  - 输入：模拟 zip，内部包含 `hardware/schematic.json`：
    - `ESP32-C3`
    - `USB-C`
    - `CH340K`
    - `VBUS / 3V3 / GND / UART_TXD / UART_RXD`
  - 输出模板：
    - `usb_power_input`
    - `mcu_power_core`
    - `uart_download_header`
  - dry-run 导入结果：
    - `{"files": 1, "rows": 3, "success": 3, "fail": 0, "retry": 0}`
- 当前交接点：
  1. 继续寻找真实嘉立创工程下载 API，把真实项目从 `text_fallback` 推进到 `file_first`。
  2. 对 `.epro/.esch/.sch` 的真实格式做样本解析，提取结构化 components/nets。
  3. 为 zip 内多工程文件增加优先级策略，避免把文档 JSON 当原理图 JSON。

## 4.9 本轮新增完成（2026-04-20）
- 真实站点脚本反查结论：
  - 从 `/_next/static/chunks/app/explore/page-*.js` 中确认了项目详情 API 形态：
    - `GET /api/project/{uuid}`
  - 从站点通用脚本中确认了工程附件相关 API 形态：
    - `DELETE /api/common/project/{project_uuid}/attachments/{attachment_uuid}`
    - `POST /api/common/project/{project_uuid}/attachments/{attachment_uuid}/addDownloadCount`
  - 当前直接请求这些 API 会被 WAF `418` 拦截，尚未找到稳定的非拦截调用方式。
- 代码增强：
  - `lceda_open_source_crawler.py` 新增 `build_attachment_download_count_url(...)`
  - `extract_project_record(...)` 现在会从页面 `application/json` 脚本中递归扫描：
    - `attachments[].url`
    - `attachments[].download_url`
    - `attachments[].file_url`
    - 若只有附件 `uuid + name`，则会构造 `addDownloadCount` 候选 URL 作为后续下载入口
- 当前意义：
  - 即使真实详情页 DOM 中没有直接暴露下载链接，只要 SSR/API JSON 带有附件元数据，抓取器也能识别工程文件候选 URL。
  - 真实网络下载仍受 WAF 限制，但数据模型和入口识别已经补齐。
- 当前交接点：
  1. 继续研究真实浏览器态请求头 / cookie / 页面跳转链，尝试绕过 `418` 获取 `/api/project/{uuid}` 响应。
  2. 若可拿到项目详情 JSON，优先补 `attachments` / `images` / `proImages` 的结构化字段解析。
  3. 若 API 仍不可用，则继续从 SSR 流或内嵌 JSON 中提取更多 `uuid/path/attachments` 线索。

## 4.10 本轮新增完成（2026-04-20）
- Next Flight / SSR 项目数据抽取：
  - `lceda_open_source_crawler.py` 新增 `extract_project_payload_from_next_flight(...)`
  - 可从 `self.__next_f.push(...)` 的服务端渲染数据中提取项目 payload
  - 当前已可稳定提取：
    - `uuid`
    - `path`
    - `name`
    - `attachments`
    - 以及 `creator/owner/introduction/count/...` 等详情字段
- 项目记录增强：
  - `extract_project_record(...)` 现在会写入 `project_uuid`
  - 若 Next Flight payload 中带 `attachments`，仍会继续参与工程文件 URL 识别
- 真实项目页验证：
  - 样本：`https://oshwhub.com/li-chuang-kai-fa-ban/project_gzzvrwqn`
  - 已成功提取：
    - `project_uuid=2507dcb6e20646e29377e4abd4922bfa`
    - `path=li-chuang-kai-fa-ban/project_gzzvrwqn`
    - `name=立创·泰山派3M（RK3576)开发板`
  - 该样本当前 `attachments=[]`，因此仍无法推进到真实 `file_first`
- 结论：
  - 即使 `/api/project/{uuid}` 仍被 `418` 拦截，抓取器也已经能从 SSR 页面恢复关键项目标识。
  - 下一步应优先寻找“带附件的真实项目页”或继续寻找详情页二级数据源（例如附件表格请求、分页接口、懒加载接口）。
- 当前交接点：
  1. 在可访问的真实项目页中批量筛查 `attachments` 非空的样本。
  2. 从详情页附件表格相关脚本中反查是否存在单独附件列表 API。
  3. 若仍无附件，则尝试从 `archive/store/origin` 等 Next Flight 字段继续反推真实工程下载入口。

## 4.11 本轮新增完成（2026-04-20）
- 批量筛查可访问真实项目页后，已确认多个 `attachments` 非空样本：
  - `https://oshwhub.com/micespring/esp32-nesemu`（attachments=8）
  - `https://oshwhub.com/jixin/USB_TTL_PL2303HXD-a6b1e2b29902455a8c64281de3ec3245`（attachments=4）
  - `https://oshwhub.com/hmtang/based-on-cm1048-cm10104-string-2`（attachments=6）
  - `https://oshwhub.com/hmtang/schematic1`（attachments=7）
  - `https://oshwhub.com/ydi_pcb/ydi-flapping-wing-flight-control-main-control-board-open-source`（attachments=2）
- 附件宿主名修正：
  - Next Flight `attachments[].src` 中的 `/oshwhub/project/attachments/...` 实际下载宿主为 `https://image.lceda.cn`
  - 已修复抓取器的附件 URL 解析规则
- 真实 `file_first` 下载验证已成功：
  - 样本：`https://oshwhub.com/hmtang/schematic1`
  - 提取到：
    - `project_uuid=0c8697374e754290ab43b9b5984bb7ce`
    - `schematic_file_url=https://image.lceda.cn/oshwhub/project/attachments/ca2c374450344c8fb9b4b3557340764f.sch`
  - 已成功落盘：
    - `results/lceda_open_source_raw/files/oshwhub.com-hmtang-schematic1-2f69568f/ca2c374450344c8fb9b4b3557340764f.sch`
  - 当前抽取结果：
    - `status_indicator`（`source=lceda_open_source_extract`, `quality_score=0.82`）
- 结论：
  - 真实项目页 → SSR payload → 附件 `src` → 真实下载 已形成首个可复现 `file_first` 闭环。
  - 当前主要瓶颈已从“找不到真实下载入口”转移到“如何从 `.sch/.epro/.esch` 中提取更高价值的结构化模板”。
- 当前交接点：
  1. 优先解析真实 `.sch` 文本结构，补 MCU/电源/UART/I2C 等模板识别。
  2. 针对 `attachments` 非空项目批量跑下载与模板抽取，建立真实样本集。
  3. 如果 `src` 指向 zip，再结合 4.8 的 zip 扫描链路继续扩大 `file_first` 覆盖面。

## 4.12 本轮新增完成（2026-04-20）
- 真实 `.sch` 文本结构解析已落地首版：
  - `extract_lceda_templates.py` 新增 `parse_schematic_text_tokens(...)`
  - 当前会从 `.sch/.esch/.epro/.json` 文本中提取结构化信号：
    - `nets`
    - `part_values`
    - `part_numbers`
    - `footprints`
    - `connectors`
  - 已覆盖真实样本里稳定可见的关键信号形态：
    - `VBAT`
    - `UART`
    - `NTC 10K`
    - `LED RED`
    - `TEST-PAD1.5MM`
    - `LED0603-RED`
- 模板抽取已接入结构化 token：
  - 不再只依赖整页文本关键字。
  - 已支持从真实 `.sch` 的 `VBAT + LED + 电阻/封装` 组合提升为 `power_indicator`。
  - 同时收紧了 `status_indicator` 规则，避免“只因文件里出现 LED 字样”而误报通用状态灯模板。
- 真实样本结果提升：
  - 样本：`https://oshwhub.com/hmtang/schematic1`
  - 之前产物：
    - `status_indicator`
  - 当前产物：
    - `power_indicator`
  - 说明：真实 `file_first` 闭环已从“能下载 `.sch`”推进到“能基于 `.sch` 结构化信号抽出更贴近电路语义的模板”。
- 测试与验证：
  - 新增测试：
    - `.sch` token 解析
    - 基于 `.sch` token 的 `power_indicator` 抽取
  - 回归命令：
    - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline`
  - 结果：
    - `Ran 26 tests ... OK`
- 当前交接点：
  1. 继续把 `.sch` token 解析扩展到更高价值组合：`VBAT/B+/B- + 0.005R + NTC + MOS`，尝试抽 `battery_protection` / `current_sense` 类模板。
  2. 对其他 `attachments` 非空真实项目批量跑 token 解析，沉淀更多真实格式特征。
  3. 若遇到 `.zip` 主体为多张 `.sch`，增加文件优先级裁剪，避免把辅助页优先当主原理图。

## 4.13 本轮新增完成（2026-04-20）
- `.sch` 结构化信号继续扩展到电池保护组合：
  - `parse_schematic_text_tokens(...)` 已补充识别：
    - `B+`
    - `B-`
    - `P+`
    - `P-`
  - 对真实文件里常见的 `B-1/B-2/B-3`、`VBAT`、`NTC1/NTC2/NTC3`、`MMBT5401/MMBT5550`、`0.005R` 等信号已能稳定参与后续规则判断。
- 模板抽取新增：
  - `battery_protection`
  - 触发组合当前收口为：
    - 电池母线信号：`VBAT` / `B+` / `B-`
    - 温度检测：`NTC*`
    - 再叠加分流电阻或保护三极管信号：`0.005R` / `MMBT*`
- 新增测试：
  - `.sch` battery protection token 解析
  - 基于 `.sch` token 的 `battery_protection` 抽取
- 验证结果：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 52 tests ... OK`
- 真实样本结果继续提升：
  - 样本：`https://oshwhub.com/hmtang/schematic1`
  - 当前抽取结果：
    - `power_indicator`
    - `battery_protection`
  - 说明：该真实 BMS 样本已不再只停留在“电源指示灯”层面，开始抽出与保护板语义更贴近的模板。
- 当前交接点：
  1. 继续把 `battery_protection` 拆细为更具体子类型或补充 `current_sense` / `temperature_sense` 派生模板，避免一个大模板承载过多语义。
  2. 基于多个真实 `attachments` 非空项目校准阈值，避免把普通电源板误判为 BMS。
  3. 若后续要让插件消费该新类型，需要同步收口 `plugin/src/editor/apply-plan/devboardRagTemplates.ts` 的模板类型白名单与映射策略。

## 4.14 本轮新增完成（2026-04-20）
- `battery_protection` 已进一步拆出两个派生模板，降低单个大模板承载过多语义的问题：
  - `current_sense`
  - `temperature_sense`
- 新增 `current_sense` 触发规则：
  - 检测到 `0.005R`
  - 且检测到 `B-` / `B-*` 低侧电流检测相关网络
  - 输出组件包含：
    - `sense_resistor`
    - `kelvin_connection`
- 新增 `temperature_sense` 触发规则：
  - 检测到 `NTC*` 网络
  - 且检测到 `NTC` 或 `10K` 阻值信号
  - 输出组件包含：
    - `ntc_thermistor`
    - `bias_resistor`
- 真实样本结果继续提升：
  - 样本：`https://oshwhub.com/hmtang/schematic1`
  - 当前抽取结果：
    - `power_indicator`
    - `battery_protection`
    - `current_sense`
    - `temperature_sense`
- 测试与验证：
  - 新增测试：
    - 基于 `.sch` token 的 `current_sense` 抽取
    - 基于 `.sch` token 的 `temperature_sense` 抽取
  - 回归命令：
    - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：
    - `Ran 54 tests ... OK`
- 当前交接点：
  1. 用更多 `attachments` 非空真实项目批量验证 `current_sense` / `temperature_sense` 的误报率。
  2. 若误报可控，再把这些新模板类型同步到插件端白名单和补全策略。
  3. 如果误报偏高，优先增加 `BMS/保护板/电池串数/MOS` 等上下文门槛，而不是继续扩大关键词。

## 4.15 本轮新增完成（2026-04-20）
- 已补电池类模板的误报防护门槛：
  - 新增 `_has_bms_context(...)`
  - `battery_protection` / `current_sense` / `temperature_sense` 现在除原理图信号命中外，还要求项目上下文包含明确 BMS 语义，例如：
    - `BMS`
    - `battery`
    - `锂电`
    - `电池`
    - `保护板`
    - `电池组`
- 已新增误报防护测试：
  - 无 BMS 语义上下文的通用电源板样本，即使文件里出现：
    - `B-`
    - `NTC1`
    - `0.005R`
    - `MMBT5401`
  - 也不会再产出：
    - `battery_protection`
    - `current_sense`
    - `temperature_sense`
- 离线批量抽取检查结果：
  - `results/lceda_open_source_raw/esp32_projects.jsonl`：
    - 无模板
  - `results/lceda_open_source_raw/explore_projects.jsonl`：
    - 无模板
  - `results/lceda_open_source_raw/sample_projects.jsonl`：
    - `mcu_power_core=2`
    - `uart_download_header=1`
  - `results/lceda_open_source_raw/hmtang_schematic1.jsonl`：
    - `power_indicator=1`
    - `battery_protection=1`
    - `current_sense=1`
    - `temperature_sense=1`
  - 结论：在当前本地真实/半真实样本集下，电池类模板没有向 MCU 开发板或 explore 样本外溢。
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 55 tests ... OK`
- 当前交接点：
  1. 下一步应优先补更多真实 `attachments` 非空项目样本，扩大误报评估样本量；当前本地样本仍偏少。
  2. 若继续沿主线推进，可以先新增一个批量评估脚本，把“输入项目 JSONL -> 输出模板分布/风险报告”固化下来。
  3. 在样本量足够前，不建议立刻把新模板类型接到插件自动补全默认白名单。

## 4.16 本轮新增完成（2026-04-20）
- 已新增批量评估脚本：
  - `scripts/server/evaluate_lceda_template_extraction.py`
  - 能直接对项目 JSONL 运行抽取评估，不依赖先落盘模板 JSONL。
- 当前评估脚本输出内容：
  - `total_projects`
  - `projects_with_templates`
  - `template_type_counts`
  - `source_mode_counts`
  - `risk_counts.battery_templates_without_bms_context`
- 已补测试：
  - `summarize_template_extraction(...)` 的分布/风险统计测试
- 本地样本评估结果：
  - `results/lceda_open_source_raw/sample_projects.jsonl`
    - `mcu_power_core=2`
    - `uart_download_header=1`
    - `battery_templates_without_bms_context=0`
  - `results/lceda_open_source_raw/hmtang_schematic1.jsonl`
    - `power_indicator=1`
    - `battery_protection=1`
    - `current_sense=1`
    - `temperature_sense=1`
    - `battery_templates_without_bms_context=0`
  - `results/lceda_open_source_raw/explore_projects.jsonl`
    - 无模板
    - `battery_templates_without_bms_context=0`
- 命令示例已写入 `scripts/server/README.md`，可直接复用：
  - `python scripts/server/evaluate_lceda_template_extraction.py --input ... --output ...`
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 56 tests ... OK`
- 当前交接点：
  1. 下一步最直接的是继续补更多真实 `attachments` 非空项目样本，再用该评估脚本统一跑风险分布。
  2. 如果要进一步工程化，可让评估脚本输出“按项目列出的命中模板清单”，便于人工抽样复核。
  3. 样本量扩大前，插件端仍不建议默认消费新电池类模板。

## 4.17 本轮新增完成（2026-04-20）
- 批量评估脚本已补“按项目命中模板清单 + 风险明细”输出：
  - `project_reports[*].project_id`
  - `project_reports[*].project_url`
  - `project_reports[*].title`
  - `project_reports[*].source_mode`
  - `project_reports[*].template_types`
  - `project_reports[*].risk_flags`
- 已补测试：
  - 项目级模板明细输出
  - 项目级 `battery_templates_without_bms_context` 风险标记
- 本地评估结果已可直接用于人工抽样：
  - `sample_projects.jsonl`
    - `ESP32-S3 开发板` -> `mcu_power_core`, `uart_download_header`
    - `RP2040_Minimal` -> `mcu_power_core`
  - `hmtang_schematic1.jsonl`
    - `基于中颖SH3676010B 7-10串 30A 锂电池保护板`
    - -> `power_indicator`, `battery_protection`, `current_sense`, `temperature_sense`
  - 当前所有项目级 `risk_flags=[]`
- 文档已更新：
  - `scripts/server/README.md` 已补 `project_reports[*].template_types` / `risk_flags` 说明
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 57 tests ... OK`
- 当前交接点：
  1. 现在可以直接扩充真实项目样本池，并把评估 JSON 作为人工复核输入。
  2. 若要继续自动化，可进一步给评估脚本增加“仅输出命中风险项目”或“按模板类型聚类的项目列表”。
  3. 在更大样本集上验证前，插件侧仍不建议默认启用新电池类模板。

## 4.18 本轮新增完成（2026-04-20）
- 已修复真实样本扩充时暴露的抓取器稳健性问题：
  - 现象：单个附件 URL 返回 `404` 时，`lceda_open_source_crawler.py` 会直接中断整批抓取。
  - 修复：附件下载失败时改为：
    - 输出告警日志
    - 当前项目自动降级为 `text_fallback`
    - 清空不可用的 `schematic_file_url` / `schematic_file_path`
    - 继续后续项目，不再中断整批任务
- 已补测试：
  - 附件下载失败时整批抓取不崩溃，且当前项目正确降级到 `text_fallback`
- 已完成一轮新的真实样本扩充与评估：
  - 输出项目集：
    - `results/lceda_open_source_raw/real_attachment_projects.jsonl`
  - 评估结果：
    - `results/lceda_template_corpus/real_attachment_eval.json`
- 本轮真实样本集结果：
  - 共 `5` 个项目
  - `projects_with_templates=2`
  - `source_mode_counts`：
    - `text_fallback=2`
    - `file_first=3`
  - 模板分布：
    - `status_indicator=1`
    - `power_indicator=1`
    - `battery_protection=1`
    - `current_sense=1`
    - `temperature_sense=1`
- 项目级结论：
  - `基于ESP32的NES游戏掌机`
    - 附件不可用，已降级为 `text_fallback`
    - 当前无模板
  - `USB-TTL串口通信模块-PL2303HXD`
    - 附件不可用，已降级为 `text_fallback`
    - 当前抽到 `status_indicator`
  - `基于CM1048+CM1010A,4串16.8V带均衡锂电保护板`
    - `file_first`
    - 当前未抽到模板，说明真实 `.sch` 规则仍有漏检空间
  - `基于中颖SH3676010B 7-10串 30A 锂电池保护板`
    - `file_first`
    - 抽到 `power_indicator` / `battery_protection` / `current_sense` / `temperature_sense`
  - `YDIFLY——蝴蝶扑翼机飞控主控板开源`
    - `file_first`
    - 当前未抽到模板
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 58 tests ... OK`
- 当前交接点：
  1. 继续优先分析 `CM1048+CM1010A 4串保护板` 为什么 `file_first` 仍未命中模板，补齐真实 BMS 规则。
  2. 检查 `YDIFLY` 的 zip 内容，判断是应抽模板还是应保持 0 输出。
  3. 对 `USB-TTL` 样本的 `status_indicator` 做一次人工复核，确认是否为合理命中还是偏噪声。

## 4.19 本轮新增完成（2026-04-20）
- 已补齐 `CM1048+CM1010A` 这类真实 BMS 样本的漏检规则：
  - 原问题：
    - 真实 `.sch` 中的 `B+ / B- / P-` 常被二进制字节包裹，现有 token 提取未稳定命中。
    - 真实电流采样电阻并不总是 `0.005R`，还存在 `0.06R` 这类低阻值。
  - 已修复：
    - 总线检测增加对原始文本的直接扫描，支持 `B+ / B- / P+ / P-` 被 `\\x00` 包裹的情况。
    - 电流采样检测从“只认 `0.005R`”扩展为“低阻值采样电阻模式”，例如：
      - `0.005R`
      - `0.06R`
    - 模板输出中的 `sense_resistor` 也改为回填真实检测值，而不是固定写死。
- 已补测试：
  - `CM1048/CM1010A + 0.06R + NTC + MOS` 真实组合抽取测试
  - `B+ / B- / P-` 被二进制字节包裹时的抽取测试
- 真实样本结果已提升：
  - `基于CM1048+CM1010A,4串16.8V带均衡锂电保护板`
  - 之前：
    - 无模板
    - 或仅 `temperature_sense`
  - 当前：
    - `battery_protection`
    - `current_sense`
    - `temperature_sense`
- 更新后的真实批量评估结果：
  - `results/lceda_template_corpus/real_attachment_eval.json`
  - 当前 5 个项目中：
    - `projects_with_templates=3`
    - `battery_protection=2`
    - `current_sense=2`
    - `temperature_sense=2`
    - `power_indicator=1`
    - `status_indicator=1`
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 60 tests ... OK`
- 当前交接点：
  1. 继续检查 `YDIFLY` 的 zip 内容，确认是否应有模板输出，还是应保持 0 命中。
  2. 对 `USB-TTL` 样本的 `status_indicator` 做人工复核，判断是否需要再收紧 `status_indicator`。
  3. 若后续开始接插件侧，应优先接入 `battery_protection/current_sense/temperature_sense` 的白名单与低置信度策略，而不是直接默认自动应用。

## 4.20 本轮新增完成（2026-04-20）
- 已完成 `YDIFLY` zip 内容复核，确认当前 0 模板命中为合理结果：
  - 样本文件：
    - `results/lceda_open_source_raw/files/oshwhub.com-ydi-pcb-ydi-flapping-wing-flight-control-main-control-board-open-source-4c56a374/60c7a8e1bff24de1b2c5378fb54f5b2d.zip`
  - 解包结果仅包含：
    - `readme.txt`
    - `YDIFly.ino`
  - 不包含 `.sch/.esch/.epro/.json` 原理图工程文件，因此抽取器保持 0 输出符合预期。
- 已完成 `USB-TTL` 样本人工复核并收紧 `status_indicator` 规则：
  - 问题确认：
    - `USB-TTL串口通信模块-PL2303HXD` 的 `status_indicator` 命中来源于页面文案“指示灯指示工作状态”，不属于 MCU 开发板语义，存在噪声风险。
  - 规则收口：
    - `status_indicator` 触发新增 `anchor_family` 前置门槛（需识别到 `ESP32/RP2040/STM32` 等主控家族）。
    - 结果：非 MCU 文本样本不再因“指示灯/status”文案触发 `status_indicator`。
- 已补回归测试：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - 非原理图 zip（`readme + .ino`）保持 0 模板输出测试。
    - 非 MCU 文本样本不产出 `status_indicator` 测试。
    - MCU 文本样本仍可产出 `status_indicator` 测试（防止功能回退）。
- 更新后的真实批量评估结果：
  - `results/lceda_template_corpus/real_attachment_eval.json`
  - 当前 5 个项目中：
    - `projects_with_templates=2`（由 3 降到 2）
    - `battery_protection=2`
    - `current_sense=2`
    - `temperature_sense=2`
    - `power_indicator=1`
    - `status_indicator=0`（已消除 `USB-TTL` 噪声命中）
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 63 tests ... OK`
  - `python3 scripts/server/evaluate_lceda_template_extraction.py --input results/lceda_open_source_raw/real_attachment_projects.jsonl --output results/lceda_template_corpus/real_attachment_eval.json`
  - 结果：`projects_with_templates=2`，`status_indicator=0`
- 当前交接点：
  1. 继续扩充 `attachments` 非空真实项目样本，优先覆盖 MCU 开发板与通用模块混合场景，验证 `status_indicator` 收紧后的召回率变化。
  2. 在更多真实 `file_first` 项目上继续沉淀 `.sch/.esch/.epro` 结构化 token 规则，优先提升非 BMS 类模板（如 `mcu_power_core` / `uart_download_header`）的文件抽取能力。
  3. 样本量达标后，再评估是否将 `battery_protection/current_sense/temperature_sense` 以低置信度策略接入插件白名单。

## 4.21 本轮新增完成（2026-04-20）
- 已推进 `file_first` 场景下的非 BMS 模板召回增强（聚焦 `mcu_power_core` / `uart_download_header`）：
  - `parse_schematic_text_tokens(...)` 追加可识别网络信号：
    - `3V3`
    - `5V`
    - `TXD`
    - `RXD`
    - `DTR`
    - `RTS`
  - `mcu_power_core` 新增结构化触发条件：
    - 在已识别主控家族前提下，若命中 `3V3/VCC/VBUS + GND` 网络组合，即使文本缺少“电源/开发板”等关键词，也可产出。
  - `uart_download_header` 新增结构化触发条件：
    - 在已识别主控家族前提下，若命中 UART 信号（`UART*` 或 `TXD/RXD/DTR/RTS`），且满足以下之一即可产出：
      - 文本存在桥接芯片线索（`CH340/CP210/PL2303/FT232/USB转UART`）
      - 连接器 token 命中 `UART` 且为 `HEADER/CONN/PAD` 类连接器
  - 同时保留防误报收口：
    - 仅有 `UART_TXD/UART_RXD` 网络、但无桥接线索且无 UART 连接器时，不会产出 `uart_download_header`。
- 已补回归测试：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - 纯原理图信号（无文本关键词）可同时触发 `mcu_power_core` 与 `uart_download_header`。
    - 无桥接线索时不会误产出 `uart_download_header`。
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 65 tests ... OK`
  - `python3 scripts/server/evaluate_lceda_template_extraction.py --input results/lceda_open_source_raw/real_attachment_projects.jsonl --output results/lceda_template_corpus/real_attachment_eval.json`
  - 结果：真实 5 项样本统计保持稳定（`projects_with_templates=2`，`battery_templates_without_bms_context=0`）。
- 当前交接点：
  1. 扩充真实 `file_first` 的 MCU 开发板样本（优先带 `CH340/CP210/FT232` 或 UART 下载头）验证新规则实际召回增益。
  2. 针对 `.zip` 多文件项目增加“主原理图优先级裁剪”（例如优先包含 `SCH/SCHEMATIC/MAIN` 语义路径），避免辅助文件干扰抽取。
  3. 在样本规模扩大后，评估是否把 `mcu_power_core/uart_download_header` 的 file-first 结果作为更高优先级输入给插件侧模板补全。

## 4.22 本轮新增完成（2026-04-20）
- 已完成一轮“真实 MCU 候选样本扩充 + 前后召回对比”：
  - 新样本集：
    - `results/lceda_open_source_raw/real_mcu_projects.jsonl`
  - 新评估结果：
    - `results/lceda_template_corpus/real_mcu_eval.json`
  - 对比汇总：
    - `results/lceda_template_corpus/real_mcu_vs_attachment_comparison.json`
- 本轮抓取入口（8 个）：
  - `https://oshwhub.com/BOOW/esp32-s3`
  - `https://oshwhub.com/embeddedboys/rp2040_minimal`
  - `https://oshwhub.com/micespring/esp32-nesemu`
  - `https://oshwhub.com/ydi_pcb/ydi-flapping-wing-flight-control-main-control-board-open-source`
  - `https://oshwhub.com/li-chuang-kai-fa-ban/project_gzzvrwqn`
  - `https://oshwhub.com/htx-studio/project_kpilekig`
  - `https://oshwhub.com/jixin/USB_TTL_PL2303HXD-a6b1e2b29902455a8c64281de3ec3245`
  - `https://oshwhub.com/hmtang/schematic1`
- 抓取与评估结果摘要：
  - 新批次 `total_projects=8`
  - `projects_with_templates=3`
  - `source_mode_counts`：
    - `text_fallback=6`
    - `file_first=2`
  - 模板分布：
    - `mcu_power_core=2`
    - `uart_download_header=1`
    - `power_indicator=1`
    - `battery_protection=1`
    - `current_sense=1`
    - `temperature_sense=1`
    - `status_indicator=0`
- 与基线（`real_attachment_projects.jsonl`）对比：
  - 项目数：`5 -> 8`（`+3`）
  - 命中项目数：`2 -> 3`（`+1`）
  - 命中率：`0.40 -> 0.375`（`-0.025`）
  - `file_first` 数：`3 -> 2`（`-1`）
  - `file_first` 占比：`0.60 -> 0.25`（`-0.35`）
  - `mcu_power_core`：`0 -> 2`（`+2`）
  - `uart_download_header`：`0 -> 1`（`+1`）
  - `status_indicator`：`0 -> 0`（保持 0，未引入噪声回归）
- 样本级观察：
  - 两个入口附件下载仍返回 `404`，自动降级 `text_fallback`（整批未中断）：
    - `micespring/esp32-nesemu`
    - `jixin/USB_TTL_PL2303HXD...`
  - 当前“非 BMS 模板召回提升”已在真实样本集中体现（`mcu_power_core` / `uart_download_header` 出现），但 `file_first` 占比仍偏低，瓶颈仍在附件可用性与下载稳定性。
- 本轮执行命令：
  - `python3 scripts/server/lceda_open_source_crawler.py ... --output results/lceda_open_source_raw/real_mcu_projects.jsonl --file-output-dir results/lceda_open_source_raw/files`
  - `python3 scripts/server/evaluate_lceda_template_extraction.py --input results/lceda_open_source_raw/real_mcu_projects.jsonl --output results/lceda_template_corpus/real_mcu_eval.json`
  - `python3 scripts/server/evaluate_lceda_template_extraction.py --input results/lceda_open_source_raw/real_attachment_projects.jsonl --output results/lceda_template_corpus/real_attachment_eval.json`
- 当前交接点：
  1. 下一步优先补“可复现高成功率”的 `file_first` MCU 项目池（至少 10 个），避免对 `text_fallback` 样本比例过高导致召回评估偏差。
  2. 优先实现 zip 内多工程文件“主原理图优先级裁剪”策略，提升 `file_first` 下的有效模板密度。
  3. 如需继续对比，建议新增同口径指标：仅统计 `file_first` 子集的模板命中率（剔除下载失败降级噪声）。

## 4.23 本轮新增完成（2026-04-20）
- 已完成“同口径 file-first 子集评估”工程化落地：
  - 更新脚本：
    - `scripts/server/evaluate_lceda_template_extraction.py`
  - 新增输出字段：
    - `file_first_subset.total_projects`
    - `file_first_subset.projects_with_templates`
    - `file_first_subset.hit_rate`
    - `file_first_subset.template_type_counts`
- 已补测试覆盖：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增断言：
    - `summarize_template_extraction(...)` 在混合样本与风险样本下都能正确返回 `file_first_subset` 统计。
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 65 tests ... OK`
- 评估产物已重算：
  - `results/lceda_template_corpus/real_attachment_eval.json`
  - `results/lceda_template_corpus/real_mcu_eval.json`
  - `results/lceda_template_corpus/real_mcu_vs_attachment_comparison.json`
- 新增同口径对比结论（仅 file-first 子集）：
  - 基线（`real_attachment_projects`）：
    - `file_first_subset.total_projects=3`
    - `file_first_subset.projects_with_templates=2`
    - `file_first_subset.hit_rate=0.6667`
  - 新批次（`real_mcu_projects`）：
    - `file_first_subset.total_projects=2`
    - `file_first_subset.projects_with_templates=1`
    - `file_first_subset.hit_rate=0.5`
  - 结论：
    - 新批次总体 `mcu_power_core/uart_download_header` 召回虽增加（来自 `text_fallback`），但在 `file_first` 同口径上尚未体现增益，核心瓶颈仍是可下载附件覆盖与有效文件命中。
- 当前交接点：
  1. 继续扩充“可稳定下载附件”的 MCU 项目池，把 `file_first_subset.total_projects` 拉高到至少 10，再评估同口径召回变化。
  2. 优先推进 zip 内“主原理图优先级裁剪”（路径语义 + 文件内容打分），降低 `file_first` 下空命中。
  3. 在 `file_first` 子集召回稳定提升前，不建议把非 BMS 新模板默认提升为自动补全高优先级。

## 4.24 本轮新增完成（2026-04-21）
- 已实现 zip 内“主原理图优先级裁剪”首版（路径语义 + 内容信号打分）：
  - 脚本：
    - `scripts/server/extract_lceda_templates.py`
  - 变更点：
    - `zip` 扫描不再无差别拼接所有文本文件；
    - 新增候选文件评分机制，综合以下因素：
      - 扩展名优先级（`.sch/.esch/.epro` 高于 `.json`）
      - 路径语义（`hardware/schematic/main/sheet` 加分）
      - 文档噪声路径（`readme/doc/bom/preview/image/...` 减分）
      - 内容电路信号（`VBAT/VBUS/3V3/GND/UART/TXD/RXD/...`）与工程字段（`PART NO/PCB DECAL`）加分
    - 抽取器仅消费评分最高的前 `3` 个候选文件，降低辅助文件干扰。
- 已补回归测试：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增场景：
    - 同一个 zip 中存在 `docs/readme.json` 与 `hardware/main.sch` 时，优先使用主原理图，稳定命中：
      - `mcu_power_core`
      - `uart_download_header`
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 66 tests ... OK`
- 当前意义：
  - `file_first` 管线在“多文件 zip”场景下的抗噪声能力提升，减少被 `readme.json` 等非电路文件稀释信号的风险。
  - 为下一步扩大真实 `file_first` MCU 样本池提供更可靠的抽取基础。
- 当前交接点：
  1. 继续扩充“可稳定下载附件”的 MCU 项目池（目标 `file_first_subset.total_projects >= 10`），再跑同口径评估。
  2. 在扩样后重点观察 `file_first_subset` 中 `mcu_power_core/uart_download_header` 是否开始出现稳定命中。
  3. 若仍偏低，继续迭代 zip 评分策略（例如增加“主文件名优先于子模块文件名”的权重）。

## 4.25 本轮新增完成（2026-04-21）
- 已完成“可稳定下载附件 MCU 样本池”扩充并达到目标：
  - 新增候选来源：
    - `https://oshwhub.com/sitemap.xml`（过滤后生成项目候选）
    - 候选清单：`results/lceda_open_source_raw/sitemap_project_candidates.txt`
  - 新抓取产物：
    - `results/lceda_open_source_raw/sitemap_candidates_projects.jsonl`（`records=39`, `file_first=7`）
    - `results/lceda_open_source_raw/real_mcu_filefirst_pool_projects.jsonl`（`records=17`, `file_first=7`）
  - 合并去重后样本池：
    - `results/lceda_open_source_raw/real_mcu_filefirst_pool_merged_projects.jsonl`
    - 结果：`merged_total=56`, `file_first=14`（已达 `>=10` 目标）
- 已完成同口径评估：
  - 评估文件：
    - `results/lceda_template_corpus/real_mcu_filefirst_pool_merged_eval.json`
  - 关键指标：
    - 全量：`total_projects=56`, `projects_with_templates=16`
    - `source_mode_counts`：
      - `file_first=14`
      - `text_fallback=42`
    - `file_first_subset`：
      - `total_projects=14`
      - `projects_with_templates=5`
      - `hit_rate=0.3571`
      - `template_type_counts`：
        - `battery_protection=2`
        - `current_sense=2`
        - `temperature_sense=2`
        - `power_indicator=1`
        - `usb_power_input=1`
        - `mcu_power_core=1`
        - `mcu_boot_reset=1`
        - `i2c_sensor_subsystem=2`
        - `status_indicator=2`
    - 风险：
      - `battery_templates_without_bms_context=0`
- 本轮结论：
  - 样本池规模目标已达成（`file_first` 从此前小样本提升到 `14`）。
  - 但 `file_first_subset` 下 `mcu_power_core/uart_download_header` 仍偏低（分别仅 `1/0`），说明当前瓶颈已从“样本量不足”转为“文件内容命中与规则覆盖不足”。
- 当前交接点：
  1. 针对 `file_first` 但未命中的项目做“失败画像”分组（文件类型、是否为固件包、是否缺少网络标识），给出规则补强清单。
  2. 优先补 `uart_download_header` 的 file-first 规则召回（例如识别 `TX/RX/GPIO0/EN` 变体、桥接芯片别名、常见下载接口丝印）。
  3. 对 `file_first` 样本引入“有效原理图置信度”字段，区分“文件可下载但非原理图语义”的样本，避免拉低规则效果判断。

## 4.26 本轮新增完成（2026-04-21）
- 已完成 `file_first` 失败画像并落地首轮去伪命中收口：
  - 画像结论：
    - 当前 `file_first` 未命中项目中，大量附件是“固件/源码 zip”而非原理图工程文件；
    - 常见表现：zip 内主要为 `.vscode/*.json`、`library.json`、`idedata.json` 等工程配置文件。
  - 直接影响：
    - 这类文件过去会被当作“文本型工程文件”参与抽取，造成 `file_first` 场景的伪命中风险。
- 已实现规则收口（`scripts/server/extract_lceda_templates.py`）：
  1. zip 候选文件新增最小分阈值：
     - `ZIP_SCHEMATIC_MIN_SCORE=7`
     - 低分 `.json` 文档/配置文件不再参与 zip 内容拼接。
  2. `file_first` 模式新增“有效原理图信号”门槛：
     - 若存在 `schematic_file_path` 但解析不到有效结构化信号（`nets/part_values/part_numbers/connectors` 均空），则禁用纯文本启发式模板触发。
     - 仅允许真实原理图信号驱动或具备明确结构化条件的触发分支。
- 已补测试：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - 固件型 zip（仅 `board.json/.vscode/settings.json`）在 `file_first` 模式下不再触发模板（`templates=[]`）。
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 67 tests ... OK`
- 重算评估（`real_mcu_filefirst_pool_merged`）：
  - 评估文件：
    - `results/lceda_template_corpus/real_mcu_filefirst_pool_merged_eval.json`
  - 关键变化：
    - `file_first_subset.total_projects=14`（不变）
    - `file_first_subset.projects_with_templates=3`（`5 -> 3`）
    - `file_first_subset.hit_rate=0.2143`（`0.3571 -> 0.2143`）
  - 解释：
    - 指标下降属于“去伪命中”预期结果，说明收口已生效；下一阶段应在“真实原理图信号”上补召回，而不是依赖文本噪声。
- 当前交接点：
  1. 继续补 `uart_download_header` 的 file-first 结构化规则（`GPIO0/IO0/EN/RST/RTS/DTR/TX/RX` 变体 + 常见桥接芯片别名）。
  2. 针对 `file_first` 但无有效信号的样本，补“原理图有效性标签/置信度”输出，避免与规则召回混淆。
  3. 在去伪命中基线稳定后，再进行下一轮 file-first 子集召回提升评估。

## 4.27 本轮新增完成（2026-04-21）
- 已补 `uart_download_header` 的 file-first 结构化规则扩展（首轮）：
  - 脚本：
    - `scripts/server/extract_lceda_templates.py`
  - 扩展内容：
    - 网络 token 增加下载/串口常见变体识别：
      - `TX/RX/U0TXD/U0RXD`
      - `RST/RESET/EN/CHIP_EN`
      - `GPIO0/IO0/BOOT0`
    - `uart_download_header` 触发收口为：
      - UART 信号命中
      - 且下载控制信号命中（如 `GPIO0/IO0/EN/RST/BOOT0`）
      - 且存在桥接线索（`CH340/CH343/CH9102/CP210/PL2303/FT232/USB-Serial`）或 UART 类连接器
    - 桥接线索来源扩展到：
      - 文本
      - `part_values`
      - `part_numbers`
- 已补测试：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增/更新：
    - 新增“bootstrap 变体信号”样例（`U0TXD/U0RXD + GPIO0 + EN + DTR/RTS + UART-HEADER`）可命中 `uart_download_header`。
    - 既有 `uart` 正向样例补充 `GPIO0/EN`，与新门槛保持一致。
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 68 tests ... OK`
- 当前意义：
  - `uart_download_header` 从“关键词 + 基础 UART 信号”进一步收敛到“下载链路结构化信号”，为后续 file-first 真实召回提升提供更可控基础。
  - 仍需在真实 file-first 样本上验证该规则增益，当前评估尚未出现明显回升。
- 当前交接点：
  1. 针对 `file_first` 子集中 MCU 类项目，补采样检查真实 `.sch/.epro/.esch` 是否包含 `GPIO0/EN/RTS/DTR` 等下载信号，确认是“规则未覆盖”还是“样本本身缺失”。
  2. 若样本确有相关信号但命中不足，继续补网络别名（如 `IO_0/ENB/NRST/UART0_TX/UART0_RX`）与连接器丝印规则。
  3. 下一轮评估重点观察 `file_first_subset.template_type_counts.uart_download_header` 是否从 `0` 提升。

## 4.28 本轮新增完成（2026-04-21）
- 已完成 `file_first` 信号画像工程化与风险标注：
  - 脚本：
    - `scripts/server/evaluate_lceda_template_extraction.py`
  - 新增能力：
    - `file_first_subset.signal_counts`（聚合）：
      - `file_first_with_any_signal`
      - `file_first_with_uart_signal`
      - `file_first_with_uart_control_signal`
      - `file_first_with_uart_bridge_hint`
      - `file_first_uart_ready`
    - `project_reports[*].file_first_signal_profile`（项目级）
    - `risk_counts.file_first_without_schematic_signal`
    - `project_reports[*].risk_flags` 在 `file_first` 无有效信号时附加：
      - `file_first_without_schematic_signal`
- 已补测试并收口断言：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 补充了 `file_first_signal_profile`/`signal_counts` 的断言；
  - 同步调整了 `risk_flags` 断言，兼容新增风险标记。
- 测试与验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 69 tests ... OK`
- 重算评估（`real_mcu_filefirst_pool_merged`）关键画像结果：
  - `file_first_subset.total_projects=14`
  - `file_first_subset.projects_with_templates=3`
  - `file_first_subset.signal_counts`：
    - `file_first_with_any_signal=3`
    - `file_first_with_uart_signal=1`
    - `file_first_with_uart_control_signal=3`
    - `file_first_with_uart_bridge_hint=0`
    - `file_first_uart_ready=1`
  - `risk_counts.file_first_without_schematic_signal=11`
- 结论：
  - 当前瓶颈已明确：14 个 `file_first` 中有 11 个缺少有效原理图信号，属于“附件可下载但非可解析原理图语义”。
  - 在现有样本池下，`uart_download_header` 的 file-first 提升空间主要受样本可用性约束，而非单纯规则覆盖不足。
- 当前交接点：
  1. 下一步优先扩充“`file_first_with_any_signal=1`”的高质量样本，而非继续堆 `file_first` 总量。
  2. 对 `file_first_without_schematic_signal` 样本增加下游过滤/分层使用策略，避免干扰模板质量判断。
  3. 在高质量样本池上再验证 `uart_download_header`（目标先把 `file_first_uart_ready` 从 `1` 提升到 `>=3`）。

## 4.29 本轮新增完成（2026-04-21）
- 已补“高信号 file-first 子集”评估口径并完成 TDD 验证：
  - 代码：
    - `scripts/server/evaluate_lceda_template_extraction.py`
  - 新增输出：
    - `file_first_high_signal_subset.total_projects`
    - `file_first_high_signal_subset.projects_with_templates`
    - `file_first_high_signal_subset.hit_rate`
    - `file_first_high_signal_subset.template_type_counts`
  - 测试：
    - `scripts/server/tests/test_lceda_open_source_pipeline.py`
    - 新增 `test_summarize_template_extraction_reports_file_first_high_signal_subset`
- 已产出高信号样本池与复评估文件：
  - 样本池：
    - `results/lceda_open_source_raw/real_mcu_filefirst_highsignal_projects.jsonl`
  - 评估：
    - `results/lceda_template_corpus/real_mcu_filefirst_highsignal_eval.json`
  - 关键结果：
    - `total_projects=3`
    - `projects_with_templates=3`
    - `file_first_high_signal_subset.hit_rate=1.0`
    - `risk_counts.file_first_without_schematic_signal=0`
- 已完成“候选重试抓取有效性”实测（基于 `sitemap_attachment_candidates.json`）：
  - 抓取产物：
    - `results/lceda_open_source_raw/sitemap_attachment_retry_projects.jsonl`
    - `results/lceda_open_source_raw/sitemap_attachment_retry_errors.json`
  - 评估产物：
    - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
  - 结果：
    - `total_projects=18`
    - `source_mode_counts.file_first=8`
    - `file_first_subset.signal_counts.file_first_with_any_signal=1`
    - `file_first_subset.hit_rate=0.125`
    - `risk_counts.file_first_without_schematic_signal=7`
  - 说明：
    - 多候选重试机制在这批数据上主要解决“首个 URL 失败即降级”的稳定性问题；
    - 但新增 file-first 样本仍以“无有效原理图信号附件”为主，核心瓶颈仍在候选质量而非下载重试逻辑。
- 全量回归验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 71 tests ... OK`
- 当前交接点：
  1. 下一步优先在候选生成阶段做“附件名语义预过滤”（优先 `sch/schematic/project/source`，降权 `firmware/stl/外壳/ibom/readme`），提高 `file_first_with_any_signal` 比例。
  2. 将 `file_first_high_signal_subset` 作为后续模板质量评估主口径，避免低质量附件稀释规则效果判断。
  3. 在高信号样本扩充后再验证 `uart_download_header`，当前该指标仍为 `file_first_uart_ready=1`，不足以支持规则二次放宽。

## 4.30 本轮新增完成（2026-04-21）
- 已落地候选附件语义降权（crawler 侧），优先避开固件/外壳类 zip：
  - 代码：
    - `scripts/server/lceda_open_source_crawler.py`
  - 变更点：
    - `_rank_schematic_url(...)` 增加正向语义权重：`main/sch`
    - 增加负向语义降权：`firmware/固件/stl/ibom/readme/manual/doc/外壳/model/3d/image/preview/logo`
- 已补测试（TDD）：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - `test_extract_project_record_deprioritizes_firmware_zip_candidate`
  - 断言：当附件同时含 `firmware.zip` 与 `main-board.zip` 时，优先 `main-board.zip`。
- 验证结果：
  - 全量回归：
    - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
    - 结果：`Ran 72 tests ... OK`
  - 重跑 `sitemap_attachment_candidates`（18 项）后：
    - `results/lceda_open_source_raw/sitemap_attachment_retry_projects.jsonl`
    - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
    - 当前 `file_first_subset.signal_counts.file_first_with_any_signal=1`，尚未出现明显提升。
- 当前结论：
  - 候选排序策略已具备“去 firmware 噪声”能力并通过测试；
  - 但当前样本瓶颈仍主要是站点附件可访问性与附件语义本身，下一步应继续从候选来源层面提升高质量附件占比。

## 4.31 本轮新增完成（2026-04-21）
- 已继续前移候选过滤到 crawler 候选集合阶段（而非仅排序）：
  - 代码：
    - `scripts/server/lceda_open_source_crawler.py`
  - 新增：
    - `ATTACHMENT_NEGATIVE_HINTS`
    - `_is_likely_noise_attachment(...)`
  - 行为：
    - `_prioritize_schematic_urls(...)` 在去重前直接跳过明显噪声附件 URL（`firmware/stl/ibom/readme/外壳/...`）。
- 已补测试并验证：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 既有测试 `test_extract_project_record_deprioritizes_firmware_zip_candidate` 增加断言：
    - `schematic_file_url_candidates` 中不应再包含 `firmware.zip`
  - 全量回归：
    - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
    - 结果：`Ran 72 tests ... OK`
- 已重跑 `sitemap_attachment_candidates` 批次并复评估：
  - 抓取：
    - `results/lceda_open_source_raw/sitemap_attachment_retry_projects.jsonl`
  - 评估：
    - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
  - 指标：
    - `source_mode_counts.file_first=8`
    - `file_first_subset.signal_counts.file_first_with_any_signal=1`
    - `file_first_subset.hit_rate=0.125`
    - `risk_counts.file_first_without_schematic_signal=7`
- 结论：
  - 仅靠附件名语义过滤，对当前候选批次提升有限；
  - 下一步瓶颈已明确为“附件内容可解析性”，建议在下载后增加轻量内容探测（命中结构化信号才保留 `file_first`，否则主动降级并记录噪声类型）。

## 4.32 本轮新增完成（2026-04-21）
- 已落地“下载后内容探测再决定 source_mode”：
  - 代码：
    - `scripts/server/lceda_open_source_crawler.py`
  - 新增行为：
    - 下载成功后，基于 `parse_schematic_text_tokens(...)` 探测是否存在结构化原理图信号；
    - 若无信号则自动降级：
      - `source_mode: file_first -> text_fallback`
      - `schematic_file_path/schematic_file_url` 置空
      - `source_mode_reason=downloaded_file_without_schematic_signal`
    - 下载失败保持降级并标注：
      - `source_mode_reason=download_failed`
    - 下载成功且有信号时标注：
      - `source_mode_reason=downloaded_file_with_schematic_signal`
- 同步修复两个稳健性边界：
  1. 候选经噪声过滤后为空时，不再索引越界（返回空候选）。
  2. 文件后缀为 `.zip` 但内容并非合法 zip 时，内容探测降级为文本解码兜底，不中断流程。
- 已补测试并通过：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - `test_crawl_downgrades_file_first_when_downloaded_file_has_no_schematic_signal`
  - 全量回归：
    - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
    - 结果：`Ran 73 tests ... OK`
- 已重跑 `sitemap_attachment_candidates` 评估：
  - 抓取：
    - `results/lceda_open_source_raw/sitemap_attachment_retry_projects.jsonl`
  - 评估：
    - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
  - 指标：
    - `source_mode_counts.file_first=8`
    - `file_first_subset.signal_counts.file_first_with_any_signal=1`
    - `risk_counts.file_first_without_schematic_signal=7`
- 结论：
  - 当前降级链路已工程化并可解释（`source_mode_reason`）；
  - 该批次指标未变，说明问题核心仍在候选源质量，而非“下载后是否判空”逻辑缺失。

## 4.33 本轮新增完成（2026-04-21）
- 已为评估脚本补充 `source_mode_reason` 分布统计：
  - 代码：
    - `scripts/server/evaluate_lceda_template_extraction.py`
  - 新增输出字段：
    - `source_mode_reason_counts`
      - `download_failed`
      - `downloaded_file_without_schematic_signal`
      - `downloaded_file_with_schematic_signal`
- 已补测试（TDD）并通过：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - `test_summarize_template_extraction_reports_source_mode_reason_counts`
- 全量回归：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 74 tests ... OK`
- 评估结果更新：
  - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
    - `source_mode_counts`：`text_fallback=17`, `file_first=1`
    - `source_mode_reason_counts`：
      - `download_failed=9`
      - `downloaded_file_without_schematic_signal=7`
      - `downloaded_file_with_schematic_signal=1`
  - `results/lceda_template_corpus/real_mcu_filefirst_pool_merged_eval.json`
    - `source_mode_reason_counts={}`（历史数据集未包含该字段，后续需重抓生成）
- 结论：
  - 在 `sitemap_attachment_retry` 这批中，`text_fallback` 的主因可量化拆分为：
    - 下载失败（9）
    - 下载成功但无原理图信号（7）
  - 下一步应对这两类原因分开治理，而不是统一视为“file-first 覆盖不足”。

## 4.34 本轮新增完成（2026-04-21）
- 已补 `/attachments/...` 附件链接的宿主映射修复：
  - 代码：
    - `scripts/server/lceda_open_source_crawler.py`
  - 规则：
    - Next Flight 附件若为 `/attachments/...`，统一映射到：
      - `https://image.lceda.cn/attachments/...`
    - 与既有 `/oshwhub/project/attachments/... -> image.lceda.cn` 规则保持一致。
- 已补测试并通过：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - `test_extract_project_record_maps_attachments_path_to_image_host`
- 全量回归：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 75 tests ... OK`
- 重跑 `sitemap_attachment_retry` 评估结果：
  - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
  - 指标：
    - `source_mode_counts`：`text_fallback=17`, `file_first=1`
    - `source_mode_reason_counts`：
      - `download_failed=9`
      - `downloaded_file_without_schematic_signal=7`
      - `downloaded_file_with_schematic_signal=1`
- 结论：
  - `/attachments -> image` 映射在代码层已补齐，但对本批次 `download_failed` 未观察到下降；
  - 下一步应直接对 `download_failed` URL 做宿主/路径画像，区分“404 永久失效链接”与“可重写/可恢复链接”。

## 4.35 本轮新增完成（2026-04-21）
- 已为 `download_failed` 画像补充抓取记录明细字段：
  - 代码：
    - `scripts/server/lceda_open_source_crawler.py`
  - 新增：
    - `source_mode_reason_detail.failed_candidate_urls`
    - `source_mode_reason_detail.failed_candidate_count`
  - 场景：
    - 仅在 `source_mode_reason=download_failed` 时写入失败候选 URL 明细。
- 同步增强了抽取器对“伪 zip 内容”的容错：
  - 代码：
    - `scripts/server/extract_lceda_templates.py`
  - 行为：
    - `.zip` 文件若不是合法 zip（`BadZipFile`），回退为文本解码，不再中断评估流程。
- 已补测试并通过：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - `test_read_schematic_file_text_falls_back_when_zip_is_invalid`
  - 既有 download-fail 测试已增加 `source_mode_reason_detail` 断言。
  - 全量回归：
    - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
    - 结果：`Ran 76 tests ... OK`
- 重跑 `sitemap_attachment_retry` 后关键结果：
  - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
  - `source_mode_counts`：
    - `file_first=3`
    - `text_fallback=15`
  - `source_mode_reason_counts`：
    - `downloaded_file_with_schematic_signal=3`
    - `downloaded_file_without_schematic_signal=14`
    - `download_failed=0`
- 结论：
  - 当前这批样本中，“下载失败”已不是主问题；
  - 新瓶颈明确转为“下载成功但无可解析原理图信号”，下一步应画像这 14 个样本的附件内容类型并在候选阶段进一步过滤。

## 4.36 本轮新增完成（2026-04-21）
- 已将“附件名噪声过滤”前移到 payload 解析阶段（不再仅依赖 URL 路径）：
  - 代码：
    - `scripts/server/lceda_open_source_crawler.py`
  - 变更点：
    - 新增 `_is_likely_noise_attachment_name(...)`
    - 在以下两个入口按附件 `name` 提前过滤噪声附件：
      - `_iter_schematic_file_urls_from_json(...)`
      - `_extract_schematic_file_urls_from_project_payload(...)`
    - 噪声关键词补充：`焊接图`
- 测试与验证：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 全量回归：
    - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
    - 结果：`Ran 77 tests ... OK`
- 重跑 `sitemap_attachment_retry`：
  - `results/lceda_open_source_raw/sitemap_attachment_retry_projects.jsonl`
  - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
  - 当前指标：
    - `source_mode_counts`：`text_fallback=15`, `file_first=3`
    - `source_mode_reason_counts`：
      - `downloaded_file_without_schematic_signal=14`
      - `downloaded_file_with_schematic_signal=3`
    - `file_first_subset.hit_rate=1.0`（`3/3`）
- 结论：
  - 本批次指标与 4.35 基本一致，说明“仅靠附件名过滤”增益有限；
  - 下一步应转向“下载后无信号样本内容类型画像 + 更严格的信号阈值（例如必须含 nets + part_values 组合）”来继续收敛。

## 4.37 本轮新增完成（2026-04-21）
- 已完成“无信号样本内容画像”并据此收紧 `file_first` 有效信号阈值：
  - 画像结论（`sitemap_attachment_retry` 无信号样本）：
    - 大量 zip 内容以源码/构建产物为主（`.h/.c/.o/.d/.bmp/.jpg/...`），并非原理图语义；
    - 典型误判来源：PlatformIO/固件构建清单中出现少量类似网络标识文本（如 `AUTO-GENERATED`）。
- 代码收口：
  - `scripts/server/lceda_open_source_crawler.py`
  - `_has_downloaded_schematic_signal(...)` 从“任意 token 命中”收紧为“电路语义阈值”：
    - 命中 `PART NO/PCB DECAL` 或 EDA XML 结构（`<eagle>/<drawing>`）可判定有效；
    - 或存在 `part_values/part_numbers`；
    - 或核心网络组合（至少 2 个核心电路网络，或“连接器 + 核心网络”）；
    - 额外补 JSON `nets` 兜底（如 `{"nets":["3V3","GND"]}`）避免误降级。
- 新增测试并通过：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 新增：
    - `test_crawl_downgrades_file_first_for_platformio_like_build_manifest`
- 全量回归：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 78 tests ... OK`
- 重跑 `sitemap_attachment_retry` 结果：
  - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
  - `source_mode_counts`：
    - `text_fallback=16`
    - `file_first=2`
  - `source_mode_reason_counts`：
    - `downloaded_file_without_schematic_signal=11`
    - `downloaded_file_with_schematic_signal=2`
  - `file_first_subset.hit_rate=1.0`（`2/2`）
- 结论：
  - 收紧后 `file_first` 数量下降（去除了误判），但高信号 `file_first` 子集保持高质量；
  - 下一步应继续扩充“真正原理图语义”的候选来源，而非放宽信号阈值。

## 4.38 本轮新增完成（2026-04-21）
- 已补“附件 `src` 与 `addDownloadCount` API 候选并行保留”：
  - 代码：
    - `scripts/server/lceda_open_source_crawler.py`
  - 变更：
    - `_extract_schematic_file_urls_from_project_payload(...)` 在存在 `src/url` 候选时，不再丢弃同附件的 API 候选；
    - 现在会同时生成：
      - 直链候选（`image.lceda.cn/...`）
      - API 候选（`/api/common/project/{uuid}/attachments/{id}/addDownloadCount`）
    - 后续下载阶段可按候选优先级与失败重试继续尝试。
- 已补测试并通过：
  - `scripts/server/tests/test_lceda_open_source_pipeline.py`
  - 更新断言：
    - `test_extract_project_record_reads_schematic_src_from_next_flight_payload`
    - 现在要求 `schematic_file_url_candidates` 同时包含 API 候选。
- 全量回归：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 78 tests ... OK`
- 重跑 `sitemap_attachment_retry`：
  - `results/lceda_template_corpus/sitemap_attachment_retry_eval.json`
  - 当前指标：
    - `source_mode_counts`：`text_fallback=16`, `file_first=2`
    - `source_mode_reason_counts`：
      - `downloaded_file_without_schematic_signal=11`
      - `downloaded_file_with_schematic_signal=2`
    - `file_first_subset.hit_rate=1.0`（`2/2`）
- 结论：
  - API 候选并行保留已落地，但在当前样本批次上未带来可观提升；
  - 下一步应继续从“候选来源质量”而非“同附件下载路径冗余”方向推进。

## 4.39 本轮新增完成（2026-04-21）
- 已新增“高概率原理图候选筛选”脚本：
  - `scripts/server/select_lceda_schematic_candidates.py`
  - 输入：
    - `results/lceda_open_source_raw/sitemap_attachment_candidates.json`
  - 输出：
    - `results/lceda_open_source_raw/sitemap_attachment_candidates_highprob.json`
  - 规则（首版）：
    - 标题 MCU/开发板/模块/电源语义加分
    - 附件名含 `source/schematic/project/工程/源码/制作包` 加分
    - 附件名含 `firmware/固件/ibom/焊接图/外壳/stl/3d/readme/doc` 降分
    - 默认阈值：`min_score=2`
- 首轮筛选结果：
  - `input_total=18`
  - `selected_total=9`
- 基于高概率候选重跑抓取+评估：
  - 抓取产物：
    - `results/lceda_open_source_raw/sitemap_attachment_highprob_retry_projects.jsonl`
  - 评估产物：
    - `results/lceda_template_corpus/sitemap_attachment_highprob_retry_eval.json`
- 与基线对比（同批次）：
  - 基线（全 18）：
    - `source_mode_counts`: `text_fallback=16`, `file_first=2`
    - `source_mode_reason_counts`: `downloaded_file_without_schematic_signal=11`, `downloaded_file_with_schematic_signal=2`
  - 高概率子集（9）：
    - `source_mode_counts`: `text_fallback=8`, `file_first=1`
    - `source_mode_reason_counts`: `downloaded_file_without_schematic_signal=8`, `downloaded_file_with_schematic_signal=1`
  - 两者 `file_first_subset.hit_rate` 均为 `1.0`，但高概率子集尚未提升 file-first 绝对命中量。
- 结论：
  - 当前标题/附件名规则筛选可用于压缩候选规模，但对“有效 file-first 绝对产出”提升有限；
  - 下一步需从候选源本身引入更强信号（例如优先有明确原理图文件结构线索的项目页），而不是继续在同一弱信号池内做规则微调。

## 4.40 本轮新增完成（2026-04-21）
- 已执行一轮“MCU 语义入口筛选”实验（基于 `sitemap_project_candidates.txt`）：
  - 从 `45` 条候选 URL 中按 slug 语义词筛出 `15` 条（`esp32/rp2040/stm32/stc/stlink/开发板/主控/飞控/usb/dap/...`）。
  - 抓取产物：
    - `results/lceda_open_source_raw/sitemap_project_candidates_mcu_semantic_projects.jsonl`（`records=13`）
  - 评估产物：
    - `results/lceda_template_corpus/sitemap_project_candidates_mcu_semantic_eval.json`
- 与当前 `sitemap_attachment_retry` 基线对比：
  - 基线（18 条）：
    - `source_mode_counts`: `text_fallback=16`, `file_first=2`
    - `file_first_subset.signal_counts.file_first_with_any_signal=2`
  - MCU 语义入口（13 条）：
    - `source_mode_counts`: `text_fallback=12`, `file_first=1`
    - `file_first_subset.signal_counts.file_first_with_any_signal=1`
  - 两者 `file_first_subset.hit_rate` 均为 `1.0`，但 MCU 语义筛选未提升高信号 file-first 绝对数量。
- 结论：
  - 仅基于 URL 语义筛选入口仍然受限于候选池质量；
  - 下一步需要改为“候选源扩样策略”：
    - 以已验证高信号项目为种子，扩作者维度/专题页维度的新入口，而非仅在现有 sitemap 弱候选内做筛选。

## 4.41 本轮新增完成（2026-04-21）
- 已执行“种子作者入口扩样”首轮实验：
  - 入口文件：
    - `results/lceda_open_source_raw/author_seed_entry_urls.txt`
  - 入口 URL（4 个）：
    - `https://oshwhub.com/1378dm`
    - `https://oshwhub.com/carele`
    - `https://oshwhub.com/hmtang`
    - `https://oshwhub.com/mojinyinhu`
- 抓取结果：
  - `results/lceda_open_source_raw/author_seed_expansion_projects.jsonl`
  - `records=0`
- 评估结果：
  - `results/lceda_template_corpus/author_seed_expansion_eval.json`
  - `total_projects=0`
- 结论：
  - 直接作者主页 URL 当前不是有效项目列表抓取入口（未提取到可用项目链接）；
  - 下一步应切换为“可枚举项目列表入口”再做作者扩样，例如：
    - 作者项目列表页（若存在明确 `/projects` 路径）
    - 站内搜索/探索接口按作者过滤入口
    - 或从作者页内嵌 JSON/Next 数据中反查项目路径字段后再喂给 crawler。

## 4.42 本轮新增完成（2026-04-21）
- 已尝试“作者扩样 + URL 规则放宽”并完成回归收口：
  1. URL 规则放宽：
     - `scripts/server/lceda_open_source_crawler.py`
     - 支持一般作者项目路径 `/<author>/<slug>`（不再仅限 `project_` 前缀）。
  2. 噪声前缀收口（防止把资源路径误当项目）：
     - 新增非项目前缀拦截：
       - `api`
       - `assets`
       - `attachments`
       - `files`
  3. 测试：
     - 新增：
       - `test_extract_project_links_supports_author_general_project_slugs`
- 作者扩样实验结果补充：
  - `author_seed_entry_urls.txt`（4 个作者主页）抓取仍为 `records=0`。
  - 说明作者主页本身不是稳定项目列表入口；需继续找“作者项目列表页/接口”。
- 全量回归：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 79 tests ... OK`
- 结论：
  - `/<author>/<slug>` 识别能力已具备，且资源路径误判已修复；
  - 下一步核心仍是找到可枚举作者项目列表的入口，而不是继续扩主页入口。

## 4.43 本轮新增完成（2026-04-21）
- 作者种子详情页扩样实验补齐“空输出可评估”闭环：
  - 抓取输入：
    - `results/lceda_open_source_raw/sitemap_author_seed_projects.txt`（3 条 seed 项目 URL）
  - 抓取输出：
    - `results/lceda_open_source_raw/sitemap_author_seed_projects.jsonl`
    - 当前结果：`records=0`（文件已稳定生成，0 行）
  - 评估输出：
    - `results/lceda_template_corpus/sitemap_author_seed_eval.json`
    - 当前结果：`total_projects=0`
- 评估脚本稳健性增强：
  - 代码：
    - `scripts/server/evaluate_lceda_template_extraction.py`
  - 变更：
    - `evaluate_project_file(...)` 在输入 JSONL 不存在时不再抛 `FileNotFoundError`，改为返回空汇总（`total_projects=0`）并带 `input` 字段。
  - 测试：
    - 新增 `test_evaluate_project_file_returns_empty_summary_when_input_missing`
- crawler 新增失败事件输出（用于量化 WAF/网络阻塞）：
  - 代码：
    - `scripts/server/lceda_open_source_crawler.py`
  - 新增：
    - `crawl_project_records(..., failure_events=...)`
    - CLI 参数：`--failure-output`
    - 输出结构：`{"failures":[{"type","url","reason"}]}`
  - 测试：
    - 新增 `test_crawl_collects_failure_events_for_entry_fetch_errors`
  - 本轮失败画像产物：
    - `results/lceda_open_source_raw/sitemap_author_seed_projects_failures.json`
    - 统计：`total_failures=3`, `418=3`
- 验证：
  - `python3 -m unittest scripts.server.tests.test_lceda_open_source_pipeline scripts.server.tests.test_transforms_pipeline scripts.server.tests.test_rag_knowledge_crawler`
  - 结果：`Ran 82 tests ... OK`
- 结论：
  - 当前环境下作者 seed 详情页入口被 WAF（418）完全拦截，扩样路径暂不可用；
  - 后续应优先从已可抓取入口（如 sitemap/explore 可访问页）做二次候选扩展，并持续输出失败画像而不是只看 `records=0`。

## 5. 退出条件
- 双端真实宿主均完成关键链路验证。
- 认证 + Credits + RAG + LLM + Agent + 草案确认落图形成端到端可复现闭环。
- 关键能力均有测试或可复现脚本及日志证据。
